---
project_id: hermes
note_type: research-synthesis
updated_at: 2026-06-12T00:00:00Z
---
# Harness Loop Synthesis

## Cross-cutting synthesis
- Harnesses measure and constrain loops.
- Loops produce the traces that harnesses evaluate.
- Good loop design makes harnesses more legible and replayable.
- Good harness design surfaces failures in retries, termination, and control flow.

## Shared best practices
- Start with traces.
- Prefer deterministic checks where possible.
- Keep environments reproducible.
- Make control flow explicit.
- Calibrate automated judges with human review.
- Treat evals and runtime telemetry as durable artifacts.

## Shared do not dos
- Do not rely on final answers alone.
- Do not use drifting environments.
- Do not leave termination implicit.
- Do not trust judges or retries without calibration.
- Do not mix inspection, execution, and closeout without evidence.

## Emerging trends
- Trajectory-first evaluation.
- OpenTelemetry-style observability.
- Durable checkpointed execution.
- Graph-based orchestration.
- More human review and approval gates.
- Durable execution is increasingly layered either inside the framework or in an external workflow engine such as Temporal's Agents SDK integration.
- Boundary-aware telemetry is converging with trajectory-aware evaluation: OpenTelemetry is adding agent-invocation and tool-execution duration metrics plus requested reasoning-level attributes, while ADK is separating tool-trajectory scoring from final-response quality.
- Result surfaces are becoming control-plane artifacts too: OpenAI now separates final output, replay history, continuation IDs, and resumable approval state, which means harnesses and loops have to agree on more than just the final answer.
- Progress signaling is part of the same boundary story: LangGraph's heartbeat-based idle timeouts make loop liveness visible to the runtime and the harness at the same time.
- LangGraph now lets the loop choose between automatic idle refresh and strict heartbeat-only refresh, which makes progress provenance part of harness design, not just runtime design.
- OpenTelemetry's Agent Framework reference scenario widens the first-party telemetry baseline again, making function-call payload capture and usage-detail reporting part of the shared harness/loop contract.
- Benchmark reproduction is now explicitly about control surfaces as much as model capability: long-horizon search harnesses rely on programmatic tool calling, compaction prompts, and task budgets, while strict JSON-schema validation keeps handoff boundaries honest.
- Structured-output recovery is now part of the shared contract too: OpenAI's Agents SDK can recover invalid final output with a validated fallback and optional history suppression, so harnesses need to distinguish raw malformed model output from the recovered final result in traces and replay.
- Cost partitioning is becoming visible at the harness/loop boundary too: coordinator-pattern designs can move token-heavy reading into cheap workers while keeping a frontier planner on the synthesis path, which makes the savings claim depend on a rigor-matched solo control and per-thread metering.
- Replay and retry semantics are getting finer-grained: one framework may snapshot a fresh thread inline instead of seeding a stub checkpoint, while another treats storage failures as failures of the outer task attempt rather than of the inner storage call.
- Nested tool-state replay is now action-bound in OpenAI's Agents SDK, so harness/loop correctness depends on preserving the originating action even when earlier tool calls are dropped or disabled during restoration.
- ADK now makes delegation-tool names and `finish_task` explicit, while its A2A docs elevate reasoning traces, long-running tools, and artifacts to first-class boundary payloads.
- Early-exit stream teardown is now part of the same correctness story: OpenAI's Chat Completions path closes provider streams explicitly, so harnesses should assert cleanup on cancellation as well as output correctness.
- Realtime sessions now treat teardown as a bounded protocol, with cleanup waiting briefly for tracked background work before closing the transport.
- Nested agent-as-tool state is now an explicit contract surface: parent runs do not inherit conversation state automatically, so replay and harness traces need to encode the chosen state-ownership model.
- Temporal's TypeScript OpenAI Agents integration now exposes workflow-internal streaming as replay-safe, which keeps live event delivery and durable replay in the same control surface.

## Hermes-specific gap themes to track
- Need for an explicit trace/eval harness around agent behavior.
- Need for stronger loop termination and escalation policies.
- Need for durable, checklist-driven activity tracking.
- Need to compare runtime surfaces against research-backed best practices.
- Need to align Hermes tracing and eval conventions with the dedicated OpenTelemetry GenAI semantic-conventions repo.
- Need to align Hermes eval criteria with trajectory-first harnesses and boundary-aware loop telemetry.
- Need harnesses that preserve run state, retry metadata, and timeout provenance across resumptions.
- Need trace schemas that preserve agent identity consistently across both internal agent-invocation spans and tool-execution spans.
- Need trace schemas that preserve per-push timeout provenance for dynamic fan-out paths.
- Need harnesses that preserve overwrite intent across serialized state updates and checkpoint after overwrite application so replay starts from the same post-overwrite state.
- Need loop/harness documentation that preserves question and answer-format instructions through compaction on long-running research tasks.
- Need schema validation at handoff boundaries so control-flow coercion cannot hide behind apparently successful runs.
- Need harnesses that preserve both malformed structured outputs and recovery-handler outcomes, including whether fallback output was included in history.
- Need harnesses and loop replayers that preserve nested action-to-state mappings when tool lists are filtered, disabled, or reordered.
- Need trace schemas and replay artifacts that preserve delegation tool names, task-return verbs, reasoning traces, long-running tool status, and generated artifacts across cross-agent boundaries.
- Need teardown checks that verify provider streams are closed on early exit, not just that runs emit the right final output.
- Need artifact persistence tests that validate scope identifiers before path construction and preserve framework-surface labels for delegated traffic.

## Maintenance log
- 2026-06-12: split from the combined research notebook.
- 2026-06-16: the overlap is now more explicit: harnesses expose approvals, environment controls, and eval surfaces, while loop runtimes expose checkpoints, thread IDs, retries, and event-loop resumption.
- 2026-06-18: the synthesis now has a clearer split between trace-first harness evaluation and durability-oriented loop orchestration, with Temporal highlighting the workflow engine as the place to preserve control flow across crashes and pauses.
- 2026-06-19: ownership boundaries are now part of the control surface: OpenAI distinguishes delegated ownership (`handoffs`) from bounded specialist calls (`agent.asTool()`), while LangGraph separates retries, terminal error handling, human interrupts, and cooperative drain into distinct primitives; that means loop topology directly affects trace volume and approval surface area.
- 2026-06-20: OpenTelemetry's new agent/tool duration metrics and ADK's trajectory-first evaluation criteria tighten the overlap between loop boundaries, telemetry, and harness scoring.
- 2026-06-21: OpenAI's results and guardrails docs now make resumable approval state explicit even across nested multi-agent paths, and LangGraph's heartbeat-based idle timeouts connect progress signals to termination behavior.
- 2026-06-22: resumable `RunState`, attempt-aware retries, and per-send timeout overrides make execution-state provenance part of the harness/loop overlap, so Hermes needs to preserve more than final outputs when comparing agent runs.
- 2026-06-24: the latest LangGraph fault-tolerance docs sharpen the overlap further by separating `run_timeout` from heartbeat-only idle refresh and exposing `node_attempt` plus structured timeout context; Hermes harnesses should capture that provenance if they want fair comparisons of long-running runs.
- 2026-06-25: OpenAI's results/state guide now broadens the harness contract to include replay-ready histories, server-managed continuation IDs, resumable approval snapshots, and richer diagnostics; LangGraph now makes the idle-clock boundary more explicit with default auto-refresh signals versus strict heartbeat-only mode, so Hermes trace capture needs to preserve both continuation state and progress provenance.
- 2026-06-26: OpenTelemetry's GenAI repo now aligns agent identity across `invoke_agent.internal` and `execute_tool` surfaces, which sharpens the overlap between harness scoring and loop ownership correlation.
- 2026-06-27: OpenAI's eval guide now explicitly routes code-first SDK workflows through built-in tracing before graders, and LangGraph now exposes per-push `Send` timeout overrides for dynamic fan-out, so Hermes needs to preserve both high-signal traces and fan-out timeout provenance when comparing runs.
- 2026-06-29: OpenTelemetry's GenAI reference repo added native Agent Framework coverage and mock Responses payloads for function calls and usage details; Hermes should keep that telemetry shape in mind when comparing harness traces and loop ownership.
- 2026-06-30: LangGraph's overwrite fixes sharpen the harness/loop contract around serialized state updates and sparse replay, so Hermes traces and checkpoints need to preserve overwrite intent and post-overwrite snapshot timing.
- 2026-07-02: Anthropic's agentic-search cookbook and OpenAI Agents SDK strict-schema fix sharpen the overlap around long-horizon reproduction: harnesses need compaction-aware instructions and task budgets, while loops need strict validation at handoff boundaries.
- 2026-07-04: OpenAI provider/config docs added another cross-cutting boundary: harnesses and loops now need to preserve transport/default-selection provenance, because Responses-to-Chat-Completions compatibility can silently drop fields and affect what the harness can replay or score.
- 2026-07-06: OpenAI's invalid-final-output recovery handler adds a new overlap point: harnesses now need to preserve the malformed model output, the validated fallback, and whether that fallback was kept in history so replay and scoring stay interpretable.
- 2026-07-07: Anthropic's coordinator-pattern cookbook plus LangGraph/Temporal/Google ADK updates added three new overlap points: costed planner-vs-worker splits, fresh-thread inline snapshots vs stub checkpoints, and task-attempt / root-agent ownership boundaries for storage and `output_key` persistence.
- 2026-07-08: OpenAI's nested tool-state restoration plus Google ADK's delegation and A2A updates added two new overlap points: action-bound replay state for nested tool interruptions, and explicit cross-agent payload contracts for delegation names, task returns, reasoning traces, long-running tools, and artifacts.
- 2026-07-08: OpenAI's Chat Completions early-exit cleanup added another overlap point: loop cancellation now needs explicit provider-stream teardown, not just final-output validation.
- 2026-07-09: OpenAI's realtime cleanup, tool-state docs, and Temporal's workflow-streaming update added three overlap points: deterministic teardown, explicit nested state ownership, and replay-safe live streaming inside durable workflows.
- 2026-07-10: the latest OpenAI Agents SDK release and LangGraph patch add a sharper boundary contract: harnesses should verify that run-scoped models and deferred sandbox resources are drained on teardown, that realtime timing is monotonic, and that hosted-subagent/local-tool metadata remains attributable; durable replay tests should also cover non-fresh `update_state` delta counters, not only the first checkpoint.
- 2026-07-11: the overlap now includes failure-notification and cleanup semantics: OpenAI's released interruption serialization makes resumable failure payloads testable, while Google ADK separates primary run errors from best-effort callbacks, shields toolset cleanup, and makes scheduler ownership explicit. Cross-agent harnesses should also record whether a managed subagent was invoked as a single-turn tool or transferred for turn ownership, since that choice changes trace topology and replay expectations.
- 2026-07-12: the overlap now includes runtime scheduling and configuration validation: Google ADK's tests make event-loop non-blocking behavior an executable harness property for synchronous tools, while OpenAI's provider fix makes invalid setup errors survive optimized execution. Hermes-style harnesses should test both concurrency health and configuration rejection, not only successful traces.
- 2026-07-13: the overlap now includes state-integrity transformations: OpenAI's session fixes make single-flight initialization, branch-safe metadata cleanup, and literal-safe history parsing testable loop contracts, while Google ADK's shared rewind filtering requires compaction and prompt paths to agree on the live event set. Hermes harnesses should test state invariants and stale-history exclusion, not only emitted outputs.
- 2026-07-14: the overlap now includes storage and cache-boundary contracts: Google ADK's artifact validation makes scope-safe persistence a harness property, while its cache fix distinguishes omitted prefix state from an invalid empty collection. Hermes should preserve backend identity/validation outcomes and test cache-state normalization at replay and retry boundaries.
- 2026-07-15: the overlap now includes observability density and replay-index contracts. OpenAI's Agents SDK lets a run choose whether wrapper task/turn spans are emitted, so Hermes should record trace-cardinality policy alongside results; Google ADK's indexed, event-count-aware rehydration and server-authoritative cache metadata make replay inputs, cache scope, and cache expiry explicit correctness artifacts. ADK's expanded module denylist also reinforces that configuration validation belongs in the harness boundary, while removal of deprecated event-provider setup means Hermes should target supported OpenTelemetry spans/logs/metrics rather than legacy event APIs.
- 2026-07-21: the overlap now includes comparable invocation budgets and streaming-event identity. OpenTelemetry's move from generic agent steps to per-invocation inference/tool call histograms gives Hermes a cleaner cross-runtime budget surface, while Google ADK's shared streaming event IDs and live `state_delta` events mean Hermes replay artifacts should preserve both chunk grouping and separate state-update events. Temporal's Go Google ADK integration also strengthens the external-durability pattern: deterministic workflow context, Activity-backed tools, durable approvals, continue-as-new snapshots, and replay-safe streaming are now documented for Google ADK as well as OpenAI Agents-style integrations. OpenAI v0.18.3 adds harness/loop regression targets around sensitive trace redaction, strict-schema expansion budgets, per-run computer provider isolation, and retry-safe session/conversation tracking.
- 2026-07-22: the overlap now includes negative-path observability and cross-agent payload fidelity. OpenAI's latest commits show that sensitive-data policy has to cover ordinary logs as well as trace payloads, while Google ADK's A2A fixes make metadata and byte-bearing tool outputs part of replayable cross-agent state. Hermes harnesses should add fixtures for log redaction, persisted tool confirmations, A2A metadata/byte round-trips, and telemetry flush barriers before treating a loop as production-clean.
- 2026-07-23: the overlap now includes approval preconditions, simulated-user branching, and delegated-stream visibility. OpenAI JS makes invalid dynamic-approval arguments an approval-gated fail-closed path instead of letting argument-inspecting policies run on malformed state; Google ADK's samples make bounded LLM-backed user simulation and invocation-scoped rubrics first-party eval surfaces; Anthropic's live subagent cookbook shows that coordinator-only streams can hide delegated work unless per-thread event deltas are captured. Hermes harnesses should add fixtures for malformed approval arguments, simulated-user clarification loops with turn caps, per-invocation rubric IDs, assistant commentary/final phase replay, and thread-scoped subagent progress streams.
- 2026-07-24: the overlap now includes continuation lineage, privacy defaults, and negative-path state shape. OpenTelemetry's `gen_ai.request.previous_response.id` turns provider-managed continuation IDs into portable trace attributes; OpenAI Agents Python shows that retry rewinds, content-filter refusals, strict schema looseness, and structured tool errors are all harness-visible loop contracts; OpenAI Agents JS makes sensitive logging deny-by-default and distinguishes missing from inaccessible sandbox paths; Google ADK fixes partial transfer replay, combined session windows, RunConfig labels, normalized Anthropic finish reasons, and opt-in final-response tool analytics. Hermes harnesses should add fixtures for previous-response IDs, retry-scoped session rewinds, explicit refusal terminal events, structured tool-error JSON, `additionalProperties: {}` rejection, sensitive-log defaults across all logger levels, missing-vs-denied sandbox paths, partial-transfer replay filtering, combined recency/time session filters, label propagation, normalized finish reasons, and configured final-response tool attribution.
- 2026-07-25: the overlap now includes capability-bound approval continuations, privacy-safe diagnostics, and typed stream consumption. Google ADK's confirmation fix shows that human approval responses must be validated against historical function-call identity, registered tools, confirmation requirements, exact arguments, and agent authorship before the loop may continue; OpenAI's diagnostics redaction shows that replay/runtime warning logs need the same privacy harness as traces; LangGraph's v3 stream projections and Temporal's Java Workflow Streams docs strengthen the pattern of treating live progress streams as typed, durable control surfaces. Hermes harnesses should add fixtures for forged approval continuations, privileged config overrides, realtime/RunState diagnostic redaction, streamed-STT dtype rejection, sandbox device nodes, v3 projection availability, and WorkflowStream state across continue-as-new.
- 2026-07-26: the overlap now includes tool-callable representation and harness-idempotence details. OpenAI's callable-object function-tool fixes mean Hermes should generate tool schemas and replay invocations from ordinary functions, async callable instances, partials, variadic signatures, and context-bearing calls, while separating model arguments from live runtime context in logs. OpenAI JS's published SDK behavior profiles show that real-model integration harnesses can be split into core semantic and capability suites. Google ADK's follow-up allowing `extra_body` narrows the previous privileged-config lesson to transport endpoint rejection plus targeted privileged-field checks, and its analytics test updates reinforce explicit flush barriers over timing sleeps.
- 2026-07-27: the overlap now includes hosted programmatic inner loops. OpenAI's Programmatic Tool Calling guide and Agents Python v0.19.0 make generated JavaScript a distinct orchestration layer inside the Responses loop, so Hermes harnesses should preserve program items, caller links, reasoning/program outputs, program fingerprints, route choice, and direct-tool baselines while loop code keeps authorization, approval-sensitive actions, replay ordering, and client-owned tool execution outside the hosted runtime. The same update tightens teardown/retry semantics: pre-response WebSocket overloads are retryable, partial overloads remain unsafe, streaming error closeout must tolerate released agent references, and JS cancellation should persist checkpoints and wait for background cleanup before declaring completion.
- 2026-07-28: the overlap now includes sibling-task cleanup, closeout batching, and per-invocation replay hygiene. OpenAI's latest Python fixes show that a harness should test empty-turn streaming, falsey provider routing, guardrail-task cancellation, bounded trace flush batches, and privacy-safe sandbox/realtime diagnostics; OpenAI Agents JS v0.14.0 turns program-call replay and deny-by-default sensitive logging into released JS baselines while extending cancellation propagation into streamed function tools and MCP requests. Google ADK's latest commits add current-invocation replay filtering, branch-isolated delegated tasks, typed missing-session append failures, single-flight backend-mode detection, transitive dependency constraints, and BigQuery analytics redaction/drain semantics. Hermes harnesses should add fixtures for sibling-task cancellation, exporter batch limits on shutdown, invocation-scoped replay, delegated branch IDs, typed state-loss errors, single-flight tool detection, dependency constraints, and analytics storage redaction before treating loop closeout as reliable.
- 2026-07-29: the overlap now includes symmetric cancellation and sandbox trust revalidation. OpenAI's Python fixes make both guardrail-first and model-first failure orderings cancellation-sensitive, and its sibling enablement-check helper makes fail-fast fan-out setup a harness property; OpenAI JS's Docker resume and cross-platform path-grant fixes show that persisted sandbox state is not enough without revalidated trust and grant manifests. Hermes harnesses should add bidirectional guardrail/model cancellation races, enablement-check fan-out cancellation, falsey builder/provider/zero-value configuration fixtures, streamed retry counters without terminal usage, valid-item replay-window tests, native path-grant normalization, Docker session trust-state revalidation, ADK schema-cache mutation-isolation checks, concrete eval-criterion serialization, event-loop-scoped resource cleanup, and optional retrieval-document telemetry.
- 2026-07-30: the overlap now includes explicit tool-span ownership, MCP elicitation, and streaming-complexity closeout. Google ADK's `gen_ai.agent.name` on `execute_tool` spans gives Hermes a first-party ADK signal for tool ownership; MCP elicitation callbacks add another resumable human/out-of-band control path; DataAgent regional routing, Apigee request timeouts, and OAuth refresh fallback make endpoint/auth provenance part of tool-loop reproducibility; and linear text/audio aggregation shows that high-chunk streaming performance belongs in harness fixtures, not only in runtime benchmarks. Hermes harnesses should add ADK tool-span agent-name assertions, MCP elicitation pause/resume fixtures, expired-token refresh/fallback cases, regional endpoint and request-timeout provenance checks, high-chunk text/audio streaming stress tests, CLI telemetry consent/session-boundary checks, and OpenAI Agents JS v0.14.1 sandbox path-grant release pins.
- 2026-07-31: the overlap now includes immutable raw replay state, cleanup-before-retry transitions, plugin-aware eval fidelity, and stricter cross-agent trust boundaries. OpenAI's realtime, tracing, voice, MCP, memory, sandbox, and schema-trimming fixes show that harnesses need fixtures for wall-clock jumps, raw-vs-normalized server events, cancellation/idle voice closeout, sanitized MCP reconnects, closed memory sessions, rejected ephemeral archive members, and keyword-collision tool parameters. Google ADK's eval, session, code-executor, structured-output, model-capability, skill-registry, A2A, and realtime fixes show that loop replay must include App plugins, exact epoch/tie-break ordering, hard process-group cleanup, validation-feedback correction turns, declared model capabilities, path-segment validation, remote card URL constraints, and explicit audio-stream-end signals. LangGraph's segment-aware namespace matching adds another state-isolation fixture for Hermes graph stores. Hermes should add focused harness cases for plugin-included eval runs, DST/tied-timestamp replay, `num_recent_events=0`, process-tree timeout cleanup, peer-metadata filtering, A2A RPC allowlists, and namespace sibling isolation before relying on these surfaces in production automation.
- 2026-08-01: the overlap now includes a distinct "fetch prior response" telemetry boundary and JS parity for several sandbox/realtime/MCP safety contracts. OpenTelemetry's `gen_ai.get_response.client` span means Hermes traces should separate historical response retrieval from inference and avoid double-counting token usage when replay/audit code fetches stored Responses by ID. OpenAI Agents JS v0.14.2 makes raw-vs-validated realtime events, ephemeral archive rejection, credential-redacted MCP metadata, cleanup-before-reconnect, and runtime-only sandbox secret resolution released JS baselines, not Python-only lessons. Hermes should add cross-SDK fixtures for response-fetch spans, raw event preservation, sanitized exported MCP identity, reconnect cleanup ordering, and trust-gated sandbox secret references; ADK's fallback OAuth lookup, rubric normalization, and defaulted output-schema fixes add smaller but useful eval/auth negative cases.
- 2026-08-02: the overlap now includes ownership-aware telemetry and paginated setup loops. OpenTelemetry's reference guidance says agent-framework instrumentation should not re-emit inference spans delegated to provider/client libraries, which means Hermes needs trace validation that distinguishes agent/tool/workflow ownership from model-call ownership to avoid duplicate token/cost accounting. OpenAI Agents Python and JS now both expose MCP pagination as a setup-loop correctness surface, so Hermes should add multi-page tool discovery, cursor-cycle, per-page retry, and aggregate metadata/cache consistency fixtures. OpenAI Agents Python v0.19.2 plus same-day fixes also make streamed guardrail-result parity, closed-default-loop `run_sync` recovery, field-specific zero-value semantics, and bound-user apply-patch move cleanup part of the harness/loop contract.
- 2026-08-03: the overlap now includes replay-order integrity, call-event typing, and tool-execution ownership. OpenAI Agents Python's ordered deduplication and handoff-stream fixes mean Hermes should preserve call-before-output order and keep handoffs out of ordinary tool-called counters. OpenAI Agents Python/JS strict-schema and sandbox snapshot fixes add setup-boundary fixtures for typeless closed object schemas and path-resolution safety. OpenAI Agents JS realtime and session fixes mean Hermes should test text-delta output guardrails, approved-tool-result persistence after output guardrail failure, and single-flight conversation-session creation. OpenTelemetry's execute-tool reference update narrows span ownership further: Hermes traces should emit tool spans only for actually executed tools while preserving separate control/delegation spans for declared or delegated work.
- 2026-08-04: the overlap now includes persisted terminal-state evidence, compaction markers, remote-agent creation ownership, and eventually consistent durable listings. OpenAI Agents Python v0.19.3 means Hermes should treat max-turn handler outputs, guardrail tripwire results, committed streamed-tool records, response-scoped realtime interruption, voice listener-error cleanup, and provider-stream closeout as released harness/loop fixtures. OpenAI Agents JS compaction and causal-order commits mean Hermes should preserve `compaction_item_created` / `RunCompactionItem` markers and call-before-output order across turns. OpenTelemetry's expanded `create_agent` reference coverage means Hermes should emit create-agent spans for remote/server-side agent services but not for local object construction, while Temporal Visibility docs mean Hermes schedule/worker/deployment list checks must account for eventual consistency and shared Visibility rate limits.
- 2026-08-05: the overlap now includes transactional state mutation, fail-fast sibling lifecycles, and clearer workflow/MCP telemetry ownership. OpenAI Agents Python v0.19.4 plus producer/consumer and sandbox env-resolver fixes mean Hermes should test streaming/event-handler/worker closeout for cancellation and deadlock freedom, not just emitted outputs. OpenAI Agents JS session transactions and blocked-output persistence mean Hermes should preserve idempotent append/replace-suffix history mutations, repeated-history provenance, executed tool side effects, hidden final outputs, and owner-scoped replay evidence when guardrails block responses. OpenTelemetry's rename to `gen_ai.invoke_workflow.duration` and MCP client/server status split mean Hermes traces and dashboards need workflow-invocation metric migration and responsibility-aware MCP error classification. Temporal's Python Google GenAI guide adds another external-durability baseline: Gemini/Vertex loops can follow the same Workflow/plugin plus Activity-backed tools pattern already tracked for OpenAI Agents, Google ADK, and LangGraph integrations.
- 2026-08-06: the overlap now includes atomic replay-state mutation, adapter-order fidelity, and routing/latency provenance. OpenAI Agents Python's multi-backend atomic session mutations and Chat Completions request-ID propagation mean Hermes should persist logical session transitions and provider request handles as first-class trace/eval evidence. OpenAI Agents JS v0.14.3 plus AI SDK fixes mean Hermes adapters need ordered reconstruction of interleaved reasoning/text/tool outputs, privacy-safe final-output validation failures, and release-pinned JS loop baselines. Google ADK's parallel `task_completed` fix and typed-dict/model-routing updates mean Hermes should test order-insensitive completion signals, typed map schemas, and new model-family route fixtures. OpenAI Fast-mode documentation also makes service tier part of harness comparability and loop scheduling rather than an opaque provider option.
- 2026-08-08: the overlap now includes invocation-bound authorization, integrity-preserving durable state, and lineage-aware telemetry. OpenAI's concrete-invocation approval binding means harnesses and loops must share the same pending-call identity before resumption; Google ADK's bounded retry jitter, complete parallel tool results, and hardened artifact publication make retry and storage transitions executable correctness properties; and OpenTelemetry's workflow conversation IDs plus client-operation reference metrics give Hermes portable lineage and request-level evidence without conflating client operations with inference. Tool descriptions/definitions must also be treated as sensitive telemetry surfaces.
- 2026-08-09: the overlap now includes durable input admission and policy-controlled replay. OpenAI Agents Python's identity-bearing pending input and server-acceptance commit point mean harnesses must score staged/admitted/accepted/committed input states separately, while its unsafe-replay approval API makes retry safety a policy-visible loop transition rather than an implicit backoff decision. Orphaned-output pruning, shell-output resume, and serialized JS compaction further make call/result pairing and compaction mutation durability shared harness/loop invariants. OpenTelemetry's pinned conformance runner reinforces that these invariants should be tested through executable fixtures with declared expected signals, not only static documentation.
- 2026-08-10: the overlap now includes single-flight teardown and replayable grading. OpenAI Agents Python/JS MCP managers serialize cleanup and bound lifecycle waits, making resource closeout a shared harness/loop state transition; JS durable pending input and structured tool-output restoration extend replay invariants across SDKs. Google ADK's resolved eval-config boundary and SWE-bench's log-only report command reinforce that reproducible harnesses must persist setup decisions and separate execution from re-grading. Hermes should model cleanup outcome, resolved evaluation config, and structured replay state as first-class run artifacts.
- 2026-08-11: the overlap now includes release-gated state evolution and explicit completeness accounting. OpenAI Agents Python v0.20.0 and JS v0.15.0 make durable pending input, structured replay, and unsafe-replay policy cross-SDK baselines, while JS's historical RunState corpus turns migration compatibility into an executable harness concern. Google ADK's failed-eval persistence and OpenTelemetry's executable workflow reference scenarios connect loop failure artifacts to telemetry validation; SWE-bench's silent-drop fixes reinforce that harness closeout must reconcile every input instance to an explicit outcome. Hermes should treat state-version corpora, failed-attempt artifacts, telemetry reference checks, and input-to-outcome accounting as one release-gate surface.
- 2026-08-12: the overlap now includes deterministic scenario injection and selective observability. OpenAI Agents JS's scripted model/sandbox utilities make replayable loop scenarios a first-party harness primitive, while LangGraph's node-level trace policy makes trace density an explicit control-flow choice. Python stream closeout and ADK evaluation-storage path validation add negative-path teardown and persistence-isolation checks. Hermes should combine scripted state-transition tests, terminal resource-close assertions, validated persistence identifiers, and declared per-node trace policy in the same release gate.
- 2026-08-13: the overlap now includes contract-stable test infrastructure and branch-safe control state. OpenAI's public testing-contract fixture and JS Standard Schema support make test doubles and schema adapters explicit compatibility surfaces; Python's interruption-state isolation and bounded MCP backoff make copy safety and wall-clock retry ceilings release-gate properties. Google ADK's no-metric crash failure and skill-telemetry goldens reinforce that evaluation completeness and attribution must be validated even when execution fails before ordinary metrics appear. Hermes should combine state-isolation, retry-ceiling, test-helper contract, and no-metric-failure fixtures in the same harness/loop gate.

- 2026-08-14: the overlap now includes independent terminal evidence and cancellation ownership. OpenAI Agents Python/JS checkpoint fixes make interruption and nested tool-decision state scope-bound across branches; Google ADK's bounded fan-out cancellation drain makes sibling cleanup part of loop completion; and SWE-bench's test-command exit-code cross-check prevents parser-visible success from overriding process failure. Hermes should preserve checkpoint scope IDs, cancellation-drain outcomes, and raw command exit status alongside parsed results in release-gate artifacts.
- 2026-08-15: the overlap now includes provider-neutral test contracts and delegated-boundary hygiene. OpenAI Agents Python/JS releases make scripted models/sandboxes and stable testing APIs first-class harness surfaces, while Google ADK makes nested RunConfig propagation and bounded live-tool shutdown explicit loop contracts. Hermes should keep deterministic fixtures versioned, record intentional config transformations at delegation boundaries, and treat orphan-task drain plus untrusted-input isolation as release-gate evidence.
- 2026-08-16: the overlap now includes attempt-scoped deadlines, sandbox scope, and independent benchmark evidence. OpenAI Agents Python/JS model-call timeouts require harnesses to preserve timeout-versus-caller-cancellation provenance and state-safe retry decisions; run-scoped sandbox cwd/network settings require explicit scope and revalidation checks; Google ADK's capped ranked memory retrieval makes context growth a loop/harness budget; and SWE-bench's positive execution markers plus retained task-repository identity make parser output and re-grading provenance separate release-gate artifacts.
- 2026-08-17: the overlap now includes pre-execution mutation and build-topology validation. OpenAI's sandbox patch grammar rejects update actions without real hunks, while SWE-bench refuses Modal runs paired with task repositories that cannot participate in the remote image build. Hermes harnesses should validate action shape and source/build identity before side effects, and retain those rejection reasons as first-class run evidence.
- 2026-08-18: the overlap now includes authorization scope, state deduplication, and isolated evaluation configuration. OpenAI's workflow-validation policy makes probe approval and recursive repository/evidence integrity explicit before execution or fingerprinting; Google ADK's per-run metric registry and equality-based event deduplication make evaluator isolation and duplicate-delivery semantics replayable; and skill-resource telemetry plus BigQuery validation add attributable sub-operation and security-boundary fixtures. Hermes should record approval scope, repository identity, metric configuration, duplicate-event handling, and resource/tool validation outcomes as shared run artifacts.
- 2026-08-20: the overlap now includes failure accounting, immutable setup state, and cost-aware durable execution. OpenAI Agents Python/JS preserve usage on failed or truncated model attempts and isolate cached MCP schemas; OpenTelemetry's modality/cache/phase breakdowns require aggregate/subset-aware accounting; Google ADK makes canonical call pairing, duplicate-execution prevention, child-interpreter isolation, and local credential ownership explicit loop boundaries; and Temporal adds namespace/workload cost attribution plus anomaly detection to durable operations. Hermes should retain billed usage and terminal failure evidence, snapshot mutable tool definitions, key tool results by call identity, and attach cost dimensions to durable run artifacts.

- 2026-08-22: the overlap now includes contract-gated releases, replay-safety classification, and audience-separated live progress. OpenAI's public API-contract fixture and unsafe-to-replay truncated-response state make compatibility and retry policy jointly testable; Google ADK's migration/action preservation and opt-in cache telemetry make storage fidelity and telemetry privacy explicit harness boundaries; and direct user-facing streaming-tool events separate progress delivery from model turns while remaining durable session history. Hermes should version public runtime contracts, preserve terminal usage/error evidence, encode replay safety as a first-class decision, and distinguish user-progress events from model-directed tool results in traces and checkpoints.
