# Project backlog bridge (legacy transform session note)

This file captured an earlier Codex-backlog bridge idea for the transform session. It is superseded by `references/project-backlog-model.md`, which defines the Hermes-native project backlog pattern.

Historical context from the session:
- Project path: /Users/bernardchase/Documents/unordered_map/priority_queue/sum/square/y_i/transform
- Hermes Kanban board: transform
- Project-specific Hermes profile: transformorchestrator

Legacy note:
- The earlier bridge pattern treated Codex as the source of truth.
- The updated model uses a Hermes project backlog as the source of truth instead.

If you need the current guidance, read `references/project-backlog-model.md`.
