# Legacy skill aliases absorbed into this umbrella

When older prompts, docs, or backlog items mention the narrower standalone skills below, treat them as part of `software-development-workflows` unless a repo-specific exception says otherwise.

## Absorbed or overlapping names
- `subagent-driven-development` → use the `Subagent execution` section in `software-development-workflows`
- `requesting-code-review` → use the `Independent review` section
- `test-driven-development` → use the `Test-first implementation` section
- `systematic-debugging` → use the `Systematic debugging` section
- `simplify-code` and `spike` → use the `Simplification and spikes` section

## Practical rule
If a lookup says a narrow skill is missing but the umbrella exists, prefer the umbrella and search its references before restoring the old file from git history.

## Compatibility note
A thin `skills/software-development/subagent-driven-development` wrapper exists in this workspace so older lookups continue to resolve while the canonical guidance stays in `software-development-workflows`.
