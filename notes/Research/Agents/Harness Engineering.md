---
project_id: hermes
note_type: research-topic
updated_at: 2026-06-12T00:00:00Z
---
# Harness Engineering

## Scope
Harness engineering covers evaluation harnesses, test harnesses, sandboxes, graders/judges, observability, reproducibility, and release gates for agentic systems.

## Search queries used
- `2025 agent evaluation harness best practices official docs instrumentation reliability release gating site:docs.openai.com OR site:platform.openai.com OR site:langchain.com OR site:docs.anthropic.com`
- `2025 evaluation harness design instrumentation reliability best practices AI agents official benchmark docs`
- `SWE-bench official docs benchmark harness agent evaluation reproduction 2025`
- `site:docs.langchain.com LangSmith tracing evals agents reliability instrumentation 2025`
- `site:developers.openai.com traces graders datasets eval runs agent workflows 2025`
- `site:anthropic.com building effective agents evals tool use traces 2026`
- `OpenTelemetry GenAI semantic conventions agent tracing 2025 official`
- `site:openai.com/index harness engineering Codex agent-first world 2026`
- `site:developers.openai.com prompt optimizer eval datasets agent workflows official`
- `site:developers.openai.com/codex/use-cases/ai-app-evals`

## Sources reviewed
- OpenAI, Harness engineering: leveraging Codex in an agent-first world
  - https://openai.com/index/harness-engineering/
- OpenAI Docs, Evaluate agent workflows
  - https://developers.openai.com/api/docs/guides/agent-evals.md
- OpenAI Codex use case, Add evals to your AI application
  - https://developers.openai.com/codex/use-cases/ai-app-evals
- Anthropic, Demystifying evals for AI agents
  - https://www.anthropic.com/engineering/demystifying-evals-for-ai-agents
- LangChain docs, Harness capabilities
  - https://docs.langchain.com/oss/python/deepagents/harness
- SWE-bench docs, The Harness
  - https://www.swebench.com/SWE-bench/reference/harness
- OpenTelemetry, Semantic conventions for generative AI systems
  - https://opentelemetry.io/docs/specs/semconv/gen-ai/
- OpenTelemetry Semantic Conventions for GenAI repo, metrics and attributes
  - https://raw.githubusercontent.com/open-telemetry/semantic-conventions-genai/main/docs/gen-ai/gen-ai-metrics.md
  - https://raw.githubusercontent.com/open-telemetry/semantic-conventions-genai/main/docs/registry/attributes/gen-ai.md
- Google ADK docs, Evaluation overview and criteria
  - https://raw.githubusercontent.com/google/adk-docs/main/docs/evaluate/index.md
  - https://raw.githubusercontent.com/google/adk-docs/main/docs/evaluate/criteria.md
- Braintrust, AI agent evaluation framework
  - https://www.braintrust.dev/articles/ai-agent-evaluation-framework
- Fiddler AI, OpenTelemetry AI observability guide
  - https://www.fiddler.ai/blog/opentelemetry-ai-observability-guide
- Greptime, OpenTelemetry GenAI semantic conventions
  - https://greptime.com/blogs/2026-05-09-opentelemetry-genai-semantic-conventions

## 2026-06-29 review addendum
- OpenTelemetry Semantic Conventions for GenAI repo, Agent Framework reference scenario commit
  - https://github.com/open-telemetry/semantic-conventions-genai/commit/b028dceecdad117461a785c3af35315e7184e813
  - Why it matters: adds native Agent Framework coverage to invoke-agent-internal, inference, and execute-tool reference scenarios and extends the mock Responses server with function-call and usage-detail payloads needed for telemetry validation.

## Sources used in synthesis
- OpenAI Harness engineering
- OpenAI Evaluate agent workflows
- OpenAI Codex evals use case
- Anthropic Demystifying evals for AI agents
- LangChain harness capabilities
- SWE-bench harness docs
- OpenTelemetry GenAI semantic conventions
- OpenTelemetry Semantic Conventions for GenAI repo
- Google ADK evaluation overview and criteria

## Knowledge developed
- Trace-first evaluation is now more useful than output-only evaluation for agent systems.
- Reproducibility depends on pinned environments, isolated runs, and stable fixtures.
- Harnesses are increasingly modular: model, tools, context, approvals, observability, and graders.
- Evals are best treated as CI gates, not one-off research artifacts.
- Telemetry is becoming boundary-aware: OpenTelemetry's GenAI conventions now define separate agent-invocation and tool-execution duration histograms plus a requested reasoning-level attribute.
- ADK's evaluation docs now separate trajectory/tool-use scoring from final-response quality and expose both reference-based and rubric-based criteria for predefined datasets.
- OpenAI's results guide now treats result surfaces as part of the harness contract: final output, replay history, continuation IDs, and resumable approval state each serve a different operational need.
- OpenTelemetry's GenAI conventions are tightening boundary identity: the latest repo alignment keeps `gen_ai.agent.name` on `execute_tool` spans while aligning `invoke_agent.internal` span and metric attributes, which makes cross-boundary trace correlation more reliable.
- OpenAI's eval guide now explicitly points code-first SDK workflows to the Integrations and observability guide for high-signal traces before graders, which reinforces trace-first harnessing for SDK apps.
- OpenTelemetry's GenAI reference repo now includes native Agent Framework coverage, so harnesses should expect agent-framework to appear alongside other runtimes in invoke-agent-internal, inference, and execute-tool reference scenarios.
- Anthropic's agentic-search cookbook shows that reproducing long-horizon benchmark scores depends on harness details such as programmatic tool calling, server-side context compaction, and explicit task budgets, not just dataset and grader selection.
- Anthropic's coordinator-pattern cookbook shows that separating planning from token-heavy reading can make cost and latency measurable harness variables, especially when the comparison includes a rigor-matched solo-frontier control and per-thread cost metering.

## Best practices
- Start with traces, not a giant prompt blob.
- Grade the workflow, not only the final answer.
- Use deterministic checks whenever possible.
- Use model-based judges where the task is subjective or open-ended.
- Keep environments isolated to reduce drift.
- Treat evals as live CI artifacts.
- Benchmark a coordinator split against a rigor-matched solo control before claiming savings from model tiering.

## Do not dos
- Do not rely on final answers alone for evaluation.
- Do not evaluate in drifting or unpinned environments.
- Do not trust model judges without calibration.
- Do not ignore latency and cost regressions.
- Do not collapse the run result to a single final answer when continuation or review state matters.

## Emerging trends
- Trajectory-first evaluation is replacing single-turn scoring.
- Observability is standardizing around OpenTelemetry-style conventions.
- Harnesses are becoming productized control planes for agents.
- Durable execution and checkpoints are becoming table stakes.
- Long-horizon benchmark harnesses are treating compaction prompts, task budgets, and in-sandbox tool fan-out as first-class knobs for score fidelity.

## Open questions
- How should long-horizon success be measured without overfitting to benchmark quirks?
- What balance of human review and automated judges is appropriate for different tasks?
- Which trace schema will become the common baseline across vendors?

## Maintenance log
- 2026-06-12: split from the combined research notebook.
- 2026-06-16: LangChain's Deep Agents harness now spells out four built-in capability groups—execution environment, context management, delegation, and steering—and makes HITL approval via `interrupt_on` explicit.
- 2026-06-16: Anthropic's Claude Fable 5 launch post again treats harness design as part of the evaluation surface, noting that a minimal harness materially changed a difficult benchmark outcome.
- 2026-06-18: OpenAI's agent-eval guidance keeps trace grading first for workflow debugging, then datasets and eval runs for repeatability; the trace surface is the right place to catch tool-choice, handoff, and policy regressions before scaling benchmarks.
- 2026-06-19: OpenAI's orchestration guide now makes the ownership split explicit: use handoffs when a specialist should own the next turn, use `agent.asTool()` when a manager should keep control, and avoid splitting too early because it adds prompts, traces, and approval surfaces.
- 2026-06-20: OpenTelemetry's GenAI semantic-conventions repo added `gen_ai.invoke_agent.duration`, `gen_ai.execute_tool.duration`, and `gen_ai.request.reasoning.level`; Google ADK's conformance evaluation docs now formalize trajectory/tool-use scoring alongside final-response review.
- 2026-06-21: OpenAI's results guide now separates final output, replay history, continuation IDs, and resumable approval state; the guardrails guide also notes that approval interruptions can surface after handoffs or inside nested `agent.asTool()` calls, so harnesses need to preserve resumable state across multi-agent workflows.
- 2026-06-25: OpenAI's results/state guide now spells out richer run-item and diagnostics surfaces beyond the high-level result contract, including item-level tool and handoff records, raw model responses, guardrail results, and usage details; harnesses should keep these around for audits and deep debugging.
- 2026-06-26: reviewed the OpenTelemetry GenAI semantic-conventions repo alignment commit; appended source-backed notes on keeping agent identity consistent across `invoke_agent.internal` and `execute_tool` spans/metrics.
- 2026-06-27: reviewed refreshed OpenAI eval and observability docs; appended source-backed notes on starting code-first SDK workflows with built-in tracing before graders.
- 2026-06-29: reviewed the OpenTelemetry GenAI Agent Framework reference scenario commit; appended source-backed notes on native Agent Framework coverage, mock Responses function-call payloads, and usage-detail telemetry fields.
- 2026-07-02: reviewed OpenAI Agents SDK strict-schema handoff validation and Anthropic's agentic-search benchmark reproduction cookbook; appended source-backed notes on strict validation at control boundaries plus programmatic tool calling, server-side compaction, and task budgets for long-horizon benchmark harnesses.
- 2026-07-04: reviewed OpenAI provider/config and models docs; appended source-backed notes on SDK-wide Responses websocket defaults, harness registration defaults, strict feature validation, silent Responses-only field dropping in Chat Completions compatibility mode, and streamed tool-call buffering for compatibility-heavy harnesses.
- 2026-07-07: reviewed Anthropic's plan-big/execute-small Managed Agents cookbook; appended source-backed notes on frontier-coordinator + cheap-worker splits, per-thread cost metering, and rigor-matched solo controls for fair cost/latency comparisons.
- 2026-07-08: reviewed OpenAI Agents SDK nested tool-state restoration and Google ADK collaboration/A2A updates; appended source-backed notes on action-bound nested replay state, delegation-tool naming, `finish_task` task return semantics, and A2A reasoning/long-running-tool/artifact capabilities.
- 2026-07-10: OpenAI Agents SDK 0.18.1 and follow-on commits add released evidence that harnesses should test deterministic teardown, nested replay restoration, and early-exit stream closure; the hosted multi-agent beta also makes local-tool versus hosted-subagent boundaries and per-agent metadata new harness dimensions. LangGraph 1.2.9 fixes the metadata/counter path required for Postgres replay after non-fresh `update_state` calls.
- 2026-07-11: OpenAI Agents SDK 0.18.2 now serializes model-backed rollout interruptions as dictionaries in the released baseline, while Google ADK makes error callbacks best-effort and cancellation-safe toolset cleanup explicit. Harnesses should preserve the primary exception separately from notification failures, assert interruption payload round-trips, and test cleanup completion even when the caller is cancelled.

## 2026-07-12 review addendum
- OpenAI Agents SDK's provider fix replaces constructor assertions with `UserError` checks for incompatible client and provider arguments, and adds regression coverage including optimized Python behavior. Harnesses should exercise invalid configuration under the actual deployment mode, not rely on assertions being enabled.
- Google ADK's code-execution regression tests verify that synchronous pre- and post-processing executes on a worker thread via `asyncio.to_thread`, while another coroutine can make progress. Async harnesses should include event-loop-stall tests for every synchronous tool or executor boundary.

## Maintenance log
- 2026-07-12: reviewed the OpenAI provider-validation and Google ADK event-loop fixes; appended notes on optimization-safe configuration validation and concurrency-sensitive harness tests.
- 2026-07-13: reviewed OpenAI Agents SDK session metadata, conversation-session initialization, and realtime usage commits plus Google ADK's rewind/compaction fix; appended notes on metadata referential-integrity tests, serialized lazy initialization, privacy-safe usage summaries, and compaction fixtures that prove rewound content cannot re-enter evaluation prompts.

## 2026-07-13 review addendum
- OpenAI's AdvancedSQLiteSession fix makes `clear_session()` remove structure and usage metadata, makes `pop_item()` remove branch-local structure and stale turn-usage rows, and guards branch-pointer updates against a concurrent clear with a generation check. Harnesses should test metadata referential integrity, numbering reset after clear/pop, and concurrent clear/branch operations rather than validating only returned conversation items.
- OpenAI's realtime session changes expose typed per-response usage, modality details, cumulative session usage, and debug summaries that omit audio frames and transcript content. Realtime harnesses should assert usage accumulation and token-detail preservation while ensuring debug observability does not capture sensitive transcript payloads.
- Google ADK now shares one `_apply_rewinds` implementation between prompt construction and token/sliding-window compaction. Its end-to-end tests verify that content annulled by a rewind marker is absent from both the live request and the compaction summary. Harnesses should include rewind-plus-compaction fixtures to detect stale-history leakage.

## 2026-07-14 review addendum
- Google ADK's artifact services now validate caller-controlled `app_name`, `user_id`, and `session_id` path segments across in-memory, GCS, and file-backed implementations, rejecting empty values, null bytes, absolute paths, and traversal segments while allowing namespaced identifiers. Harnesses that persist artifacts should test both scope-preserving namespaced IDs and adversarial path inputs across every storage backend.
- Google ADK now adds a `+managed_agent` suffix to ManagedAgent request tracking headers, distinguishing managed-agent traffic from other Interactions API traffic while retaining the common `google-adk` tool name. Harnesses should preserve framework-surface labels in request and trace metadata when comparing delegated versus local agent behavior.

## Maintenance log
- 2026-07-14: reviewed Google ADK artifact path validation and ManagedAgent request labeling; appended notes on storage-scope security tests and cross-surface attribution metadata.

## 2026-07-15 review addendum
- OpenAI Agents SDK now makes automatic task and turn tracing spans configurable per run, defaulting to enabled while allowing them to be disabled for lower-cardinality traces; its tests preserve agent, generation, and function spans and verify their parent relationships when the wrapper spans are omitted. Harnesses should treat span-density policy as an explicit run configuration and test both full-fidelity and reduced-cardinality trace contracts.
- Google ADK now blocks additional OS/network module aliases (`posix`, `nt`, `_posixsubprocess`, and `_socket`) during agent-config reference validation. Config-driven harnesses should test import aliases and private-module escape routes, not only the obvious `os`, `subprocess`, and `socket` names.
- Google ADK removed OpenTelemetry event-logger provider setup because the Events API/SDK is deprecated and is being removed. Harnesses that depend on GenAI events should verify the supported logs/spans/metrics path rather than assuming an event-provider installation remains available.
- Google ADK's Gemini cache identity now includes model and backend scope, uses canonical JSON serialization, and honors the server-reported expiry when available. Cache-aware harnesses should test model/backend isolation, mapping-order stability, and server-authoritative expiry instead of relying on local TTL estimates.

## Maintenance log
- 2026-07-15: reviewed OpenAI Agents SDK tracing-span configuration and Google ADK security, telemetry, and context-cache commits; appended notes on configurable trace cardinality, alias-complete config validation, supported telemetry surfaces, and cache identity/expiry correctness.

## 2026-07-21 review addendum
- OpenTelemetry's GenAI semantic-conventions repo replaced the ambiguous `gen_ai.agent.steps` metric with per-invocation histograms for `gen_ai.invoke_agent.inference_calls` and `gen_ai.invoke_agent.tool_calls`, recommended by `gen_ai.agent.name` and counted once across the call tree. Harnesses should stop treating framework-specific "steps" as comparable and instead score model-call and tool-call budgets explicitly per agent invocation.
- OpenTelemetry's GenAI reference workflow now includes reference-evaluation instructions scoped to scenarios, reinforcing that telemetry schemas need executable reference evaluation rather than only static convention text.
- Google ADK's rubric evaluator now asks judges to echo short rubric IDs and matches verdicts by ID before falling back to normalized property text, preventing paraphrased rubric text from silently dropping all scores. Rubric-based harnesses should include stable criterion IDs in grader prompts and fail closed when expected verdict IDs are missing.
- Google ADK's streaming path now gives all partial chunks and the aggregated final event for one LLM call the same event ID while keeping function-call and function-response events independent. Streaming harnesses should group partial output by event identity instead of treating every chunk as a distinct run event.
- OpenAI Agents SDK v0.18.3 ships fixes that materially affect harness reliability: non-tool trace errors are redacted when sensitive tracing is disabled, strict JSON-schema `$ref` expansion is bounded to avoid algorithmic-complexity stalls, concurrent computer-tool provider instances are isolated per run, streamed session input is preserved across model retries, and server-managed conversation tracking avoids stale prepared-item identity reuse.

## Maintenance log
- 2026-07-21: reviewed OpenTelemetry GenAI semantic-conventions call-count metrics and reference evaluation coverage, Google ADK rubric/streaming fixes, and OpenAI Agents SDK v0.18.3 release notes; appended notes on comparable invocation-count telemetry, grader ID stability, streaming event grouping, trace redaction, schema-expansion bounds, per-run provider isolation, retry-safe streamed input, and stale-identity-free conversation tracking.

## 2026-07-22 review addendum
- OpenAI Agents SDK now redacts raw model exceptions from response and streaming error logs when model-data logging is disabled, and redacts tool-action exceptions when tool-data logging is disabled; its tests explicitly cover secret prompt/tool payloads not appearing in sibling `logger.error` calls. Harnesses should test every error-reporting surface, not only trace `SpanError` payloads, for sensitive-data policy compliance.
- OpenAI's realtime path now logs only the failed raw message type on conversion failure when model-data logging is disabled, instead of logging the raw event payload. Realtime harnesses should include malformed client-message fixtures that prove transcripts or raw media metadata cannot leak through conversion-failure logs.
- OpenAI's sandbox changes add a Vercel S3 mount strategy with a documented boundary: mounts are declared at sandbox creation, stay outside workspace persistence, are unmounted on close, and credentials remain trusted live configuration rather than serialized session state. Sandbox harnesses should verify mount lifecycle and credential non-persistence as part of workspace reproducibility.
- OpenAI's Modal workspace persistence fix preserves leading dots in skip paths such as `.venv` when building tar excludes. Workspace harnesses should include dot-prefixed skip-path fixtures so persistence filters do not silently archive or restore excluded private/cache directories.
- Google ADK now waits for BigQuery analytics `BatchProcessor.flush()` with `Queue.join()` so flush waits for dequeued-but-in-flight writes, not just an empty queue. Observability harnesses should treat plugin flush as a completion barrier and test in-flight telemetry writes before process shutdown.

## Maintenance log
- 2026-07-22: reviewed OpenAI Agents SDK error-redaction and sandbox commits plus Google ADK analytics flush changes; appended notes on log-surface redaction, create-time mount lifecycle, dotfile-preserving persistence excludes, and telemetry-drain completion barriers.

## 2026-07-23 review addendum
- OpenAI Agents Python now records MCP servers that are cancelled during connect before re-raising `CancelledError`, so `connect_all()` failure cleanup can close partially opened transports even when `async with` never reaches `__aexit__`. Harnesses should include cancelled-during-connect MCP fixtures and assert resource closure rather than relying on later global cleanup.
- OpenAI Agents Python extends sensitive-data redaction to MCP tool-call errors and tool approval rejection formatter failures when tool-data logging is disabled, logging safe error type or generic failure text instead of raw upstream/tool payloads. Harnesses should test MCP and formatter error paths alongside ordinary model/tool error logs.
- OpenAI Agents Python and JS now pin and verify rclone sandbox installs instead of piping the installer script: the runtime/Docker path downloads an architecture-specific release, checks SHA-256, and fails distinctly on checksum mismatch. Sandbox harnesses should treat third-party tool bootstrap provenance and checksum verification as part of reproducibility, especially for remote mounts.
- Google ADK's new evaluation samples make simulated-user conversations a first-party harness pattern: each eval case can use a `conversation_scenario` with `starting_prompt`, `conversation_plan`, and `user_persona`; the evaluator config chooses an LLM-backed simulator, caps turns with `max_allowed_invocations`, and pairs it with supported criteria such as hallucination and per-turn user-simulator quality. Interactive-agent harnesses should evaluate branching clarification loops with bounded simulated users, not only scripted transcripts.
- Google ADK now supports invocation-level rubrics by merging criterion rubrics with invocation rubrics, requiring at least one effective rubric after filtering, and pretty-printing by rubric ID when criterion rubrics are absent. Rubric harnesses can attach case-specific criteria directly to invocations, but should fail closed when filtering removes every rubric.
- Anthropic's Managed Agents live-streaming cookbook documents per-thread `event_deltas`, session `initial_events`, and live observation of delegated subagents. Multi-agent harnesses should capture thread-level streaming deltas so coordinator-only session streams do not hide subagent progress, latency, or failure modes.

## Maintenance log
- 2026-07-23: reviewed OpenAI Agents Python/JS sandbox and redaction fixes, Google ADK eval samples/rubric fixes, and Anthropic Managed Agents live-streaming cookbook; appended notes on MCP cleanup, checksum-pinned sandbox bootstrap, expanded negative-path redaction, simulated-user evals, invocation-scoped rubrics, and per-thread subagent observability.

## 2026-07-24 review addendum
- OpenTelemetry's GenAI semantic-conventions repo added `gen_ai.request.previous_response.id` as a recommended request attribute when a request references a previous response or interaction identifier, with OpenAI `previous_response_id` called out as an example. Harness traces should preserve continuation IDs as first-class attributes so multi-turn evals can correlate provider continuation state without depending on opt-in message payload capture.
- OpenAI Agents Python tightened several harness-visible negative paths: strict schema conversion now rejects `additionalProperties: {}` and other non-false `additionalProperties` schemas instead of treating empty mappings as strict; Chat Completions streaming now synthesizes a refusal event when a buffered streamed tool-call path ends with `finish_reason="content_filter"`; and programmatic structured-tool errors are encoded as JSON objects such as `{"error": ...}` when a tool has an output schema. Harnesses should test schema-looseness rejection, safety-refusal visibility, and typed error-output shape rather than accepting empty turns or plain-text fallback errors.
- OpenAI Agents JS now disables sensitive model/tool data logging by default with a programmatic opt-in and expanded logging helpers across MCP, memory-session, result, run, and runtime payload paths. Harnesses should treat sensitive-data logging as deny-by-default and audit ordinary logs, debug/warn sinks, MCP labels, and session dumps alongside traces.
- OpenAI Agents JS sandbox path probing now distinguishes missing workspace paths from inaccessible files with typed not-found/archive-read errors, including Docker, local workspace, Cloudflare, and skills capability paths. Sandbox harnesses should assert missing-vs-denied failure classes separately so permission regressions are not hidden as absent files.
- Google ADK's BigQuery analytics plugin now has opt-in `final_response_tool_names`, emitting an `AGENT_RESPONSE` analytics event when configured final-response tools complete successfully while preserving the default of no such event. Analytics harnesses can now capture tool-delivered final answers explicitly, but should assert the opt-in boundary and source-tool attribution.
- Google ADK's RunConfig now supports user labels and merges them into LLM request labels for billing/attribution, while AnthropicLlm responses now populate mapped `finish_reason` values in both streaming and non-streaming paths. Harnesses should verify label propagation and provider-normalized finish reasons as part of comparable run metadata.

## Maintenance log
- 2026-07-24: reviewed OpenTelemetry continuation-ID telemetry, OpenAI Agents Python strict schema/stream-refusal/structured-error fixes, OpenAI Agents JS privacy and sandbox-path commits, and Google ADK labels/finish-reason/analytics updates; appended notes on continuation correlation, negative-path visibility, deny-by-default logging, typed sandbox errors, tool-final-response analytics, and comparable attribution metadata.

## 2026-07-25 review addendum
- OpenAI Agents Python now redacts realtime server-event validation and raw-message conversion failures down to fixed operational messages when model-data logging is disabled, and adds a shared warning helper for mixed model/tool diagnostic data that avoids even formatting redacted arguments. Harnesses should test ordinary warning/error log paths with hostile `__str__`/`__repr__` values and ensure realtime event types, event IDs, transcripts, payloads, and RunState item details cannot leak when either model or tool data logging is disabled.
- OpenAI Agents Python now rejects streamed STT audio buffers unless they are `int16` or `float32`, raising `UserError` for unsupported numpy dtypes instead of silently encoding unexpected sample formats. Voice harnesses should include dtype-negative fixtures so transcription regressions fail at the SDK boundary rather than as downstream audio-quality or provider errors.
- OpenAI Agents Python sandbox `ls -la` parsing now preserves GNU and BSD character/block device nodes by treating device IDs as size-zero non-file entries instead of dropping rows whose size column is `major, minor` or hexadecimal. Sandbox harnesses that compare mounted or container file listings should include device-node fixtures, especially for `/dev` and PTY-heavy environments, so reproducibility checks do not silently miss non-regular entries.
- Google ADK now validates tool-confirmation continuations against the executing agent's registered tools, the original function-call ID in session history, exact name/argument equality, and whether the tool statically or dynamically required confirmation. Approval harnesses should include forgery and tampering cases for missing history, unregistered tools, argument/name mismatches, non-confirmation tools, and cross-agent authorship so confirmation responses cannot become an unauthorized tool-execution channel.
- Google ADK now rejects `http_options.base_url` and `http_options.extra_body` inside `LlmAgent.generate_content_config`, while still allowing request-time HTTP options such as timeout. Config-driven harnesses should fail closed when agent-level model-generation config can override transport, tools, system instructions, or response schema through extra request bodies.
- LangGraph now types `stream_events(version="v3")` and `astream_events(version="v3")` as `GraphRunStream` / `AsyncGraphRunStream` and exposes always-registered native projections for `values`, `messages`, `lifecycle`, and `subgraphs`, while opt-in projections remain behind `extensions`. Streaming harnesses can now assert projection availability and type shape directly instead of treating v3 event streams as opaque iterators.

## Maintenance log
- 2026-07-25: reviewed OpenAI Agents Python redaction, voice, and sandbox fixes; Google ADK confirmation and config-validation fixes; and LangGraph v3 stream typing. Appended notes on negative-path log redaction, dtype gates, device-node listing coverage, confirmation-forgery prevention, privileged config rejection, and typed stream projection contracts.

## 2026-07-26 review addendum
- OpenAI Agents Python now supports async callable objects as function tools, requires an explicit `name_override` when an API-safe tool name cannot be derived from the callable class, and preserves callable compatibility by ignoring annotated instance state while validating unsupported callable-object annotations before invocation. Harnesses should generate tool-schema fixtures from callable instances, callable classes with `__call__`, partials, context-bearing call signatures, and invalid generic/Self-style annotations instead of assuming ordinary functions cover the tool boundary.
- OpenAI Agents Python now strips leading stars from Google/NumPy-style `*args` and `**kwargs` docstring parameter names before mapping descriptions into generated tool schemas. Schema harnesses should include variadic-parameter docstring fixtures so tool descriptions do not disappear from model-visible schemas when teams switch docstring style.
- OpenAI Agents Python now reuses a locked verbose stdout logging handler instead of adding a fresh `StreamHandler` on every `enable_verbose_stdout_logging()` call. Observability harnesses should treat logging setup as idempotent and include repeated/concurrent enablement checks so debug logs do not multiply across long-lived workers or test processes.
- OpenAI Agents JS added published-package integration profiles that exercise real SDK behavior across Responses and Chat Completions provider parity, structured output, delegation, approval/resume, conversation state, tracing, guardrails, execution controls, hosted tools/MCP, tool search, programmatic tool calling, and hosted multi-agent caller identity. This is a useful first-party example of splitting SDK harnesses into core semantic behavior and capability profiles while keeping optional hosted/beta coverage gated by environment variables.
- Google ADK reversed the prior `generate_content_config.http_options.extra_body` rejection and now allows `extra_body` while continuing to reject `http_options.base_url` as a transport setting. Config harnesses should track the narrower invariant: agent-level config must not override transport endpoints, but request-body extensions may be permitted by the framework and need targeted tests for the specific privileged fields that remain rejected elsewhere.
- Google ADK BigQuery analytics tests now use the plugin's `flush()` barrier instead of fixed sleeps before asserting written rows, building on the earlier queue-join flush semantics. Analytics harnesses should prefer explicit flush/drain APIs over timing guesses for closeout assertions, especially under parallel test runners.

## Maintenance log
- 2026-07-26: reviewed OpenAI Agents Python callable-tool/schema/logging commits, OpenAI Agents JS published SDK behavior integration harness, and Google ADK config/analytics test commits; appended notes on callable-object tool fixtures, variadic docstring preservation, idempotent logging setup, semantic/capability integration profiles, revised `extra_body` policy, and explicit telemetry flush barriers.

## 2026-07-27 review addendum
- OpenAI's Programmatic Tool Calling guide makes direct-vs-programmatic tool routing a harness dimension: apps choose whether each eligible tool can be called directly, from generated JavaScript, or both via `allowed_callers`; harnesses should assert that the route used matches the intended workflow stage, especially for approval-sensitive writes, citations, native artifacts, and semantic judgments that the guide says should remain direct by default.
- The same guide says programmatic runs return ordinary Responses output items—`program`, nested `function_call` items with `caller` linkage, and `program_output`—and that stateless replay must preserve every output item, including program and reasoning items. Programmatic-tool harnesses should therefore score output correctness alongside replay completeness, caller linkage, and preservation of program fingerprints/reasoning state.
- OpenAI's Programmatic Tool Calling evaluation section recommends starting with direct tool calling as a baseline, then comparing final-answer correctness, completeness, evidence coverage, input/total tokens, latency, cost, model turns, tool calls, retries, recovery behavior, safety outcomes, and whether the intended route ran. Harnesses should treat any efficiency claim from hosted programmatic orchestration as unproven until measured against that direct-tool baseline.
- OpenAI Agents Python v0.19.0 releases Programmatic Tool Calling into the SDK with `ProgrammaticToolCallingTool`, per-tool `allowed_callers`, structured function-tool outputs, and integration with Runner streaming, guardrails, approvals, sessions, and `RunState`. SDK harnesses should add capability-gated tests that include programmatic nested calls, structured programmatic errors, approvals/resume, session replay, and streaming rather than treating it as a plain function-call variant.
- OpenAI Agents Python now compares `inspect` sentinels by identity in `function_schema`, with regression tests for defaults whose equality returns non-boolean containers or always `True`. Schema harnesses should include hostile/default objects so optional parameters are not silently marked required or crashed by user-defined equality.
- OpenAI Agents JS now preserves refusal message parts when converting history for the AI SDK integration, mapping refusals into ordered text parts with provider options. Compatibility harnesses should include mixed text/refusal assistant messages so safety refusals do not disappear when crossing extension/adaptor history formats.

## Maintenance log
- 2026-07-27: reviewed OpenAI Programmatic Tool Calling docs, OpenAI Agents Python v0.19.0 and function-schema/WebSocket/streaming-result fixes, and OpenAI Agents JS cancellation/refusal fixes; appended notes on route-aware programmatic-tool evaluation, replay/caller-linkage preservation, direct-tool baselines, released SDK capability gates, identity-safe sentinel fixtures, and refusal-preserving history conversion.

## 2026-07-28 review addendum
- OpenAI Agents Python now lets streamed runs pass an empty prepared input list through to the model instead of raising `RuntimeError("Prepared model input is empty")`. Streaming harnesses should include empty-list and filtered-empty fixtures so legitimate continuation or no-new-user-input turns are not rejected before provider behavior can be evaluated.
- OpenAI Agents Python now routes provider maps by checking `provider is not None` rather than provider truthiness. Provider-selection harnesses should include falsey provider objects so custom adapters cannot be skipped accidentally during model-prefix routing.
- OpenAI Agents Python now cancels the parallel input-guardrail task when the model turn fails with a non-tripwire error. Guardrail harnesses should assert that parallel guardrails are cancelled or drained on model failure rather than becoming orphaned background tasks that continue after `Runner.run` has raised.
- OpenAI Agents Python's `BatchTraceProcessor` now respects `max_batch_size` during `force_flush()` and shutdown, not only during background export. Trace-export harnesses should assert closeout batching and exporter-call counts under forced flush/shutdown paths so telemetry drains do not become oversized or provider-rejected batches.
- OpenAI Agents Python extended sensitive-data policy to Blaxel sandbox unmount logging and realtime audio-format diagnostics, redacting mount paths and raw audio-format mappings when tool/model data logging is disabled. Privacy harnesses should include sandbox-teardown and realtime-format negative paths in ordinary log auditing, not only trace payloads.
- OpenAI Agents JS v0.14.0 releases Programmatic Tool Calling support and states that program calls are preserved across streaming, sessions, replay, and serialized `RunState`, while also making sensitive model/tool data logging disabled by default. JS harness baselines should treat program-call preservation and deny-by-default logging as released behavior rather than unreleased commit assumptions.
- Google ADK now publishes Python-version-specific constraints files for transitive dependency protection, with a documented four-day buffer. Reproducibility harnesses should pin ADK test environments with the matching constraints file when validating framework behavior over time.
- Google ADK's BigQuery Agent Analytics plugin now fails closed around formatter/parser output, redacts sensitive mappings, diagnostic text, signed URIs, and raw prompt text before inline or GCS storage, validates existing table schemas recursively at startup, bounds JSON nesting consistently, uses O(1) queue accounting, and reports shutdown success only after owned work, clients, and executors are drained or closed. Analytics harnesses should test redaction before storage, schema compatibility failures, bounded nesting, and drain-complete shutdown semantics.

## Maintenance log
- 2026-07-28: reviewed OpenAI Agents Python/JS release and reliability commits plus Google ADK analytics/dependency commits; appended notes on empty streamed inputs, falsey provider routing, guardrail cleanup, trace flush batching, sandbox/realtime log redaction, JS v0.14.0 released baselines, ADK constraints, and BigQuery analytics privacy/shutdown contracts.

## 2026-07-29 review addendum
- OpenAI Agents Python v0.19.1 releases the latest path-grant, cancellation, retry-accounting, SQLite-session, trace-flush, and redaction fixes as a public baseline. Harnesses should update Python SDK baselines to include empty/falsey configuration fixtures, streamed retry accounting, and sandbox path-grant regression tests instead of treating those as source-only fixes.
- OpenAI Agents Python and JS both expanded sandbox path-grant handling: Python adds native host path support across sandbox providers and local/Docker sessions, while JS adds native Windows path grants, allows Docker workdirs inside grants, and preserves verified Docker sessions across resumes only with trust-state validation. Sandbox harnesses should cover host-native path normalization, grant-scoped workdirs, persisted grant manifests, and resume trust checks across local, Docker, and remote providers.
- OpenAI Agents Python now counts valid SQLite session items for positive history limits rather than raw database rows. Memory/replay harnesses should seed invalid or filtered rows and assert that positive recency windows return the intended valid conversational items.
- OpenAI Agents Python now preserves an explicit zero Blobfuse attribute-cache timeout instead of replacing it via falsey defaulting. Mount harnesses should include zero-valued cache and timeout knobs when validating provider mount translation.
- OpenAI Agents Python now counts streamed retries even when terminal usage is missing. Cost/retry harnesses should decouple retry counters from provider usage payload availability and assert retry accounting on streamed terminal events with absent usage.
- Google ADK now caches `FunctionTool` declarations across LLM calls while returning copies for mutation, and serializes eval criteria as their concrete subclasses. Harnesses should test schema-cache mutation isolation and persisted evaluator subtype round-trips, because both affect repeatable tool declarations and grader configuration.
- OpenTelemetry's GenAI conventions now allow retrieval document ID and score to be optional. Trace harnesses should not synthesize fake document IDs or scores just to satisfy telemetry schemas; partial retrieval evidence can be represented explicitly as missing.
- LangGraph 1.2.10 releases the previously reviewed typed v3 stream-events/native projections and `trace_policy` changes as the current package baseline. LangGraph harness baselines should pin against the release rather than an unreleased commit when asserting v3 projections or node trace-policy behavior.

## Maintenance log
- 2026-07-29: reviewed OpenAI Agents Python v0.19.1 and sandbox/retry/session commits, OpenAI Agents JS sandbox resume/path-grant commits, Google ADK tool/eval commits, OpenTelemetry retrieval telemetry, and LangGraph 1.2.10; appended notes on path grants, session windows, retry accounting, declaration caching, eval serialization, optional retrieval evidence, and released LangGraph baselines.

## 2026-07-30 review addendum
- Google ADK now adds `gen_ai.agent.name` to `execute_tool` spans when an invocation context has an agent, with functional telemetry expectations updated for normal and exception tool spans. Harnesses should assert explicit agent attribution on tool spans instead of inferring ownership from trace hierarchy alone, aligning ADK more closely with the OpenTelemetry GenAI agent/tool boundary guidance already tracked here.
- Google ADK changed streaming response aggregation and audio-cache flush from repeated string/byte concatenation to list accumulation plus join. Streaming harnesses should include long text, thought-text, progressive SSE, and audio chunk fixtures to catch quadratic accumulation regressions before they appear as latency or memory spikes in long-running evaluations.
- Google ADK's DataAgentToolset now derives Gemini Data Analytics endpoints from explicit settings, resource-name locations, global/default routing, EU/US REP endpoints, regional endpoints, mTLS variants, or a custom API endpoint; Apigee OpenAI-compatible requests now pass `http_options.timeout` through both streaming and non-streaming HTTP calls. Provider/tool harnesses should record endpoint-routing inputs and request-time timeout propagation as reproducibility metadata rather than treating all Google-backed tools as one global endpoint.
- OpenAI Agents JS v0.14.1 releases the sandbox path-grant fixes reviewed on 2026-07-29: native Windows path grants, Docker workdirs inside grants, verified Docker session preservation across resumes, and lifecycle/agent-tool helper type exports. JS harness baselines can now pin these sandbox trust and path-normalization expectations to a public release rather than source-only commits.
- Google ADK's CLI telemetry updates add command-run logging plus parent-terminal session grouping with one-hour inactivity TTL pruning. CLI harnesses that exercise developer workflows should verify telemetry consent state, command/flag capture boundaries, session grouping, queue pruning, and privacy expectations for command metadata.

## Maintenance log
- 2026-07-30: reviewed Google ADK telemetry/tool/runtime commits and OpenAI Agents JS v0.14.1; appended notes on explicit tool-span agent attribution, streaming aggregation complexity, endpoint/timeout reproducibility, released sandbox path-grant baselines, and CLI telemetry-session harnessing.

## 2026-07-31 review addendum
- OpenAI Agents Python now uses `time.monotonic()` for batch trace processor export deadlines. Observability harnesses should test scheduled export and `force_flush` behavior under wall-clock jumps so trace closeout does not stall or fire early when system time changes.
- OpenAI Agents Python now rejects Modal workspace tar members under ephemeral persistence paths during hydration instead of skipping them during validation while still extracting the raw tar. Sandbox harnesses should include malicious archive members under ephemeral/excluded paths and assert rejection before extraction.
- OpenAI Agents Python now preserves realtime raw server-event payloads while applying validation-only normalization such as converting `previous_item_id: null` to a compatibility value. Realtime harnesses should separately assert parsed event compatibility and raw-event fidelity for audit/replay.
- OpenAI Agents Python expanded MCP hardening: URL credentials are redacted from SDK errors, tracing, and tool-origin metadata; failed MCP servers are cleaned before reconnect; and nested cleanup `ExceptionGroup` paths avoid retaining sensitive values in log records. MCP harnesses should audit trace, error, tool-origin, reconnect, and cleanup surfaces for credential leakage and resource release.
- OpenAI Agents Python now enforces closed state for Redis and Dapr sessions and extends valid-item recency-window counting to Redis/Dapr in addition to SQLite-style sessions. Memory harnesses should test use-after-close rejection, idempotent close, owned-client release, corrupt-row skipping, and backend parity for positive history limits.
- OpenAI Agents Python's tool-output trimmer now preserves tool parameter names that collide with JSON-schema prose keywords such as `description`, `title`, `examples`, and `default`. Schema/trimming harnesses should include keyword-collision parameter fixtures so token-budget trimming does not silently remove callable arguments.
- Google ADK now wires `App` plugins through CLI, dev-server, local service, and generator eval paths. Eval harnesses should run both bare-agent and App-wrapped agents and assert plugin callbacks, artifact/memory/session services, and eval-run context are identical to runtime paths.
- Google ADK now makes session event reads deterministic and timezone-correct by serializing the exact event epoch, preserving stored payload timestamps over naive datetime columns, and breaking timestamp ties with event IDs across database and SQLite reads. Replay harnesses should include DST fallback, tied timestamps, and `num_recent_events` truncation fixtures.
- Google ADK now validates live/eval audio sample-rate parsing, including rejecting non-positive resampling rates and ignoring `rate` substrings in other MIME parameters. Audio harnesses should include malformed MIME and zero/negative sample-rate fixtures before resampling or live relay.
- Google ADK now kills runaway local and container code-executor processes by supervising process groups, enforcing hard wall-clock bounds, and escalating timed-out local children from SIGTERM to SIGKILL. Code-execution harnesses should verify child-process cleanup, timeout exit codes, output closure, and hostile timeout-disarming attempts.
- Google ADK now returns schema-validation feedback from `set_model_response` instead of treating invalid structured output as a validated final response. Structured-output harnesses should score both the model's correction loop after validation feedback and the absence of invalid final payloads from `get_structured_model_response`.
- Google ADK introduced model capability reporting for LLMs, including whether output schema and tools can be combined. Provider harnesses should record capability reports alongside model names and assert feature routing against reported capabilities rather than regex-only model-family inference.
- Google ADK now scopes custom eval metrics per registry and trusts custom metric code paths only from eval config. Eval harnesses should test custom metric isolation across apps/runs and reject unconfigured metric-code paths rather than relying on process-global registry state.
- Google ADK now validates skill names before building Agent Registry URLs, requiring a single safe kebab/snake-case path segment before a model-issued `get_skill` request reaches HTTP. Tool-registry harnesses should include traversal, query, fragment, revision-path, uppercase, and empty-name fixtures.
- Google ADK now ignores unsafe A2A peer-supplied `EventActions` metadata except for a small peer-settable set, and constrains RPC targets from network-fetched A2A agent cards across all advertised interfaces. Cross-agent harnesses should treat peer metadata and remote card URLs as untrusted and assert action-field filtering plus DNS/IP allowlist enforcement.
- LangGraph checkpoint-postgres and checkpoint-sqlite now scope namespace prefix matching to segment boundaries rather than raw string prefixes, with SQLite using a private namespace-match function instead of overriding caller `REGEXP`. Store harnesses should include sibling namespaces such as `foo` and `foobar`, wildcard/literal escapes, and async SQLite setup coverage.
- Temporal documentation now recommends setting explicit Worker Identity for Cloud Run worker pools because the SDK default may appear as `1@localhost`, and added a consistency checker for SDK metrics reference tables/sections. Durable-workflow harnesses should record worker identity in serverless deployments and treat metrics reference consistency as a docs-as-harness pattern for operational telemetry claims.

## Maintenance log
- 2026-07-31: reviewed OpenAI Agents Python realtime/tracing/sandbox/MCP/memory/tool-schema commits, Google ADK evaluation/session/security/code-executor/model-capability commits, LangGraph checkpoint-store namespace matching, and Temporal worker/metrics docs; appended notes on trace closeout clocks, archive rejection, raw-event fidelity, credential redaction, closed-state memory sessions, plugin-aware evals, deterministic replay ordering, code-executor cleanup, schema-feedback loops, capability reporting, A2A trust boundaries, and segment-aware store namespaces.

## 2026-08-01 review addendum
- OpenTelemetry's GenAI semantic-conventions repo added a `get_response` operation and `gen_ai.get_response.client` span for fetching a previously generated model response by identifier, including an OpenAI Responses refinement and reference scenario. Because the operation performs no inference and consumes no new tokens, harness telemetry must not report fetched response token counts to `gen_ai.client.token.usage`; those counts describe the original generation, not the fetch.
- OpenAI Agents JS v0.14.2 releases several harness-visible sandbox/realtime/MCP fixes: sandbox archive hydration rejects ephemeral paths, realtime transport listeners can preserve exact raw payloads while typed listeners use validated representations, MCP endpoint credentials are redacted from transport errors and external metadata, and MCP reconnect cleans selected servers before retrying only those whose cleanup completed. JS harness baselines should now include raw-vs-validated realtime event assertions, archive negative fixtures, sanitized MCP metadata/log checks, and cleanup-before-reconnect state.
- OpenAI Agents JS now preserves reconstructable sandbox environment secret references across persisted RunState/session envelopes while keeping resolved secrets runtime-only and binding resume to the current trusted manifest before any reference is resolved. Sandbox harnesses should test reference subtype round-trips and ensure persisted manifests contain lookup metadata, not secret values.
- Google ADK's auth handler now resolves raw fallback OAuth token strings and checks prefixless session-state keys when retrieving authentication responses. Auth harnesses should include fallback-token and key-prefix variants so credential lookup failures are not mistaken for missing user authorization.
- Google ADK's rubric evaluator now strips markdown and typographic decoration before matching rubric text, and its `set_model_response` tool no longer marks defaulted output-schema fields as required. Evaluation harnesses should include decorated rubric verdicts and defaulted schema fields so grader parsing and schema-feedback loops do not over-fail otherwise valid cases.

## Maintenance log
- 2026-08-01: reviewed OpenAI Agents JS v0.14.2, OpenTelemetry GenAI fetch-response telemetry, and Google ADK auth/eval/schema commits; appended notes on no-token fetch-response spans, JS sandbox/realtime/MCP parity, secret-reference persistence, fallback OAuth lookups, rubric normalization, and defaulted output-schema handling.

## 2026-08-02 review addendum
- OpenAI Agents Python v0.19.2 is now a released baseline for several harness-visible fixes that were previously source-level signals: monotonic trace export deadlines, raw realtime event preservation, MCP credential/error redaction and cleanup-before-reconnect, valid-item memory-session windows, Redis/Dapr closed-state enforcement, ephemeral sandbox archive rejection, tagged sandbox `EnvValue` persistence, schema-keyword-safe tool trimming, LiteLLM stream cleanup, and voice cancellation/idle-dispatch fixes. Harnesses should pin Python SDK comparisons against v0.19.2 before treating those behaviors as unreleased assumptions.
- OpenAI Agents Python now reports tool input/output guardrail results on `RunResultStreaming` and persisted streaming state, matching non-streamed `Runner.run()` behavior. Streaming harnesses should assert guardrail-result parity across streamed and non-streamed entry points, not only that rejected/allowed tool outputs appeared in `new_items`.
- OpenAI Agents Python now auto-paginates MCP `list_tools` and `list_prompts` until `nextCursor` is exhausted, retries only the failed page, caches the complete pre-filter tool list, preserves first-page prompt metadata, rejects repeated cursors, and avoids retaining opaque cursor values in error surfaces. MCP harnesses should include second-page tools/prompts, empty-string cursors, cursor cycles, per-page retry, cache/filter ordering, and cursor-redaction cases.
- OpenAI Agents JS now follows every MCP `tools/list` continuation cursor across stdio, SSE, and Streamable HTTP before publishing tools, commits aggregate tool metadata atomically with the complete tool list, restores committed metadata on failure, rejects repeated cursors, preserves empty-string cursors, and fails closed if the installed MCP client lacks aggregate metadata caching. Cross-SDK MCP harnesses should test multi-page discovery in both Python and JS so first-page-only fixtures do not hide missing tools.
- OpenAI Agents Python's explicit zero-value contract fix clarifies where zero means provider/default behavior (`FileSearchTool.max_num_results=0`, shell action timeouts, Blobfuse cache size defaults), where positive finite values are required (MCP manager lifecycle timeouts), and where `client_session_timeout_seconds=0` disables MCP read timeouts. Configuration harnesses should exercise zero, `None`, positive, non-finite, boolean, and out-of-range values separately instead of treating all falsey values as equivalent.
- OpenAI Agents Python's sandbox `apply_patch` move path now removes the moved source as the bound sandbox user and skips removal when the destination equals the source. Sandbox/file-edit harnesses should assert user attribution on read, mkdir, write, and remove operations for create/update/move/delete flows.
- OpenTelemetry's GenAI reference guidance now says a scenario should emit telemetry only for operations the library itself performs: agent frameworks should not emit inference spans when they delegate model calls to an instrumentable underlying LLM library, except when the framework is the only observable model-call boundary. Harness telemetry should validate ownership boundaries to avoid double-counting model spans and token usage across framework and provider instrumentation.

## Maintenance log
- 2026-08-02: reviewed OpenAI Agents Python v0.19.2 and same-day commits, OpenAI Agents JS MCP pagination, and OpenTelemetry GenAI reference-instrumentation guidance; appended notes on released Python baselines, streamed guardrail parity, MCP pagination across SDKs, zero-value fixture coverage, bound-user apply-patch move cleanup, and instrumentation ownership/double-count prevention.

## 2026-08-03 review addendum
- OpenAI Agents Python now preserves item order when collapsing duplicate model/session inputs and when replacing stale tool-call outputs, keeping the latest `function_call_output` after its matching `function_call`. Harnesses should include duplicate-call/output histories and assert stable replay ordering rather than only final message equality.
- OpenAI Agents Python now stops emitting handoff calls as streamed `tool_called` events. Streaming harnesses should distinguish tool execution from handoff/delegation events so tool-call metrics, approvals, and UI surfaces do not overcount delegation as ordinary tool use.
- OpenAI Agents Python now has explicit agent-tool name-collision handling: derived names that collide can raise `UserError` under an error policy, explicit `tool_name` overrides disambiguate, and warn/keep-last behavior is separately tested. Multi-agent harnesses should assert API-safe unique tool identities after normalization, especially for `Agent.as_tool()` wrappers.
- OpenAI Agents Python and JS now normalize closed typeless object schemas for strict tools. Schema harnesses should include object schemas without an explicit `type`, nullable root schemas, and closed `additionalProperties` cases so strict conversion neither rejects valid closed objects nor treats nullable roots as strict when they cannot be represented safely.
- OpenAI Agents Python and JS hardened default sandbox snapshot path resolution. Sandbox harnesses should include snapshot-root/path-normalization fixtures that prove default snapshot paths remain inside the intended resolved boundary before archive persistence or restore.
- OpenAI Agents JS now preserves approved tool results across output-guardrail tripwires and session/RunState persistence, with regression coverage for streamed and non-streamed runs. Approval/guardrail harnesses should assert that approved tool outputs remain replayable after a later output guardrail blocks the final response.
- OpenTelemetry's GenAI reference repo narrowed `execute_tool` reference coverage to libraries that actually run the tool, while adding coverage for `google-genai`, `langchain`, and `vertexai` and removing model/client libraries that merely delegate. Telemetry harnesses should validate tool-span ownership separately from inference-span ownership and avoid treating all tool-call declarations as tool-execution spans.

## Maintenance log
- 2026-08-03: reviewed OpenAI Agents Python/JS reliability commits and OpenTelemetry GenAI reference ownership commit; appended notes on replay order, event typing, tool-name identity, strict schema normalization, sandbox snapshot boundaries, guardrail persistence, and execute-tool span ownership.

## 2026-08-04 review addendum
- OpenAI Agents Python v0.19.3 is now the released baseline for several harness-visible fixes: max-turn handler outputs are persisted to sessions, output guardrail results are reported when tripwires abort runs, falsey custom output extractors and handoff input filters are honored, AsyncSQLite sessions enforce closed state and SQLite insert failures roll back, realtime output guardrails apply to text deltas, voice streams clean up when they close early, STT listener-error handling completes, streamed task spans use the run's own workflow name, AnyLLM provider streams are released while completed runs are preserved, and model-call failures are recorded on the provider's own span. Harness baselines should pin these to v0.19.3 rather than treating them as scattered source-only fixes.
- OpenAI Agents JS now preserves inline compaction items across turns by representing them as `RunCompactionItem` / `compaction_item_created`, validating compaction items, reconciling legacy compaction session state before resume, and trimming to the latest compaction marker. Long-horizon harnesses should keep compaction markers in replay/eval artifacts instead of collapsing them into ordinary reasoning or message items.
- OpenAI Agents JS also now preserves causal order when deduplicating provider-identified model inputs and resolves model-visible tool-name collisions with a default warning plus opt-in error policy. Cross-SDK harnesses should expect the same replay-order and deterministic tool-identity invariants in JS that were tracked for Python on 2026-08-03.
- Google ADK promoted `DataAgentToolConfig` and `DataAgentToolset` from experimental to stable. Harnesses that exercise Data Agent tools can now treat the API surface as stable, while still capturing the endpoint-routing and timeout provenance noted on 2026-07-30.
- Google ADK fixed LiteLLM timeout propagation by converting `http_options.timeout` from milliseconds to LiteLLM's seconds field. Provider harnesses should assert timeout-unit translation directly, because a previously configured millisecond timeout could otherwise hang far beyond the intended request budget.
- OpenTelemetry's GenAI reference repo expanded `create_agent` span coverage to Anthropic Managed Agents, AWS Bedrock Agent, Google GenAI, Mistral, Azure AI Foundry, and OpenAI Assistants, while removing erroneous local AutoGen `create_agent` emission. Telemetry harnesses should treat `create_agent` as a client operation for remote/server-side agent services, not as a generic local-agent-construction span.
- Temporal's Visibility docs now say list/search/count operations across executions—including workflow list/count, schedule list/count, batch, deployment, task-queue, namespace, worker, update, and nexus endpoint reads—use the Visibility index, are eventually consistent, and share the Visibility API rate limit; single-entity lookups by ID do not count against that rate limit. Durable-workflow harnesses that validate agent schedules or worker state through list APIs should account for eventual consistency and rate-limit provenance.

## Maintenance log
- 2026-08-04: reviewed OpenAI Agents Python v0.19.3 and same-day Python/JS commits, Google ADK Data Agent/LiteLLM commits, OpenTelemetry `create_agent` reference coverage, and Temporal Visibility docs; appended notes on released harness baselines, compaction-marker preservation, cross-SDK replay/tool-identity parity, stable Data Agent tools, timeout-unit tests, remote-agent create-span ownership, and visibility-index consistency boundaries.

## 2026-08-05 review addendum
- OpenAI Agents Python v0.19.4 is now the released baseline for several harness-visible negative paths: completed tool-guardrail results are preserved, invalid tool-argument errors are redacted, non-streaming session saves are deferred until output guardrails, non-streaming content-filter refusals are surfaced, realtime failed connection attempts are cleaned up, sandbox token output budgets are enforced, MongoDB sessions enforce closed state, repeated session-history provenance is preserved, empty `add_items` avoids remote conversation creation, branch-ID reuse is rejected, and streamed provider thinking blocks are preserved. Harness baselines should pin these behaviors to v0.19.4 rather than source-only assumptions.
- OpenAI Agents Python's producer/consumer helper now drains paired producer-consumer loops without hanging when the producer, consumer, callback, or worker fails with ordinary exceptions, `BaseException`, or cancellation. Streaming, Codex-tool, agent-as-tool, and sandbox-memory harnesses should include callback-failure and cancellation fixtures that assert no queue join or dispatch task can deadlock closeout.
- OpenAI Agents Python now supports both MCP Python SDK v1 and v2 in client compatibility tests while keeping repository examples targeted at v2. MCP harnesses should add version-matrix coverage for server startup, tool and prompt discovery, Streamable HTTP/SSE/stdio behavior, and metadata differences instead of assuming a single MCP SDK baseline.
- OpenAI Agents JS added idempotent session-history transactions and fixed repeated session-history provenance. Session harnesses should test append and suffix-replacement transactions, repeated object references, callback rewrites, and streaming/non-streaming persistence so histories neither grow duplicates nor lose provenance under retries or compaction.
- OpenAI Agents JS now preserves committed tool effects when output guardrails block final output and marks executed tool outputs explicitly. Harnesses should score blocked-output runs with separate assertions for hidden final output, persisted side effects, approval state, and replayable tool-output execution status.
- OpenAI Agents JS now redacts invalid tool-argument diagnostics by default and surfaces empty Chat Completions content-filter responses as explicit refusals in both streaming and non-streaming paths. Privacy and safety harnesses should test invalid argument payloads, nested agent tools, streamed tool calls, and empty filtered completions as visible redacted/refusal events rather than empty turns or raw error logs.
- Google ADK's GCP Agent Identity and IAM Connector auth providers now cache REST clients in thread-local storage instead of shared provider fields. Auth harnesses for concurrent tool loops should assert thread-local client separation, credential refresh/request isolation, and absence of cross-thread shared-state contention.
- OpenTelemetry's GenAI conventions renamed the workflow duration metric to `gen_ai.invoke_workflow.duration` and clarified that MCP server telemetry should not classify client-side JSON-RPC errors as server failures, while MCP client telemetry still reports JSON-RPC error codes as client errors. Telemetry harnesses should migrate workflow-duration assertions and split MCP client/server status expectations so 4xx/not-found style client errors do not inflate server failure rates.
- Temporal added a Python Google GenAI integration guide for using the Temporal Python SDK plugin to call Gemini/Vertex models durably from Workflows, including tools as Activities, structured output, MCP tool servers, streaming, files, Interactions API/managed agents, and Activity timeout/retry guidance. Durable harnesses can now compare Google GenAI plugin behavior alongside the existing OpenAI Agents, Google ADK, and LangGraph integration patterns.

## Maintenance log
- 2026-08-05: reviewed OpenAI Agents Python v0.19.4 and post-release commits, OpenAI Agents JS session/guardrail/realtime/privacy commits, Google ADK auth concurrency, OpenTelemetry workflow/MCP semantic-conventions commits, and Temporal's Python Google GenAI docs; appended notes on released baselines, deadlock-free closeout, MCP version compatibility, idempotent session transactions, blocked-output side-effect preservation, thread-local auth clients, renamed workflow metrics, MCP status semantics, and durable Google GenAI integration harnessing.

## 2026-08-06 review addendum
- OpenAI Agents Python now makes session mutations atomic across SQLite-style, SQLAlchemy, Redis, and MongoDB session implementations, with MongoDB writing each non-empty `add_items()` call as one logical-batch document ordered by a monotonically increasing sequence and failing oversized batches atomically instead of storing a partial batch. Session harnesses should add concurrent add/pop/clear/replace-suffix fixtures, backend parity checks, and oversized-batch failure assertions so retries and multi-process writers cannot produce partially persisted replay state.
- OpenAI Agents Python now propagates the OpenAI SDK request ID on the Chat Completions path, matching the debugging handle already available on Responses-style results. Provider-comparison harnesses should capture `request_id` for both Responses and Chat Completions so failed or anomalous turns can be correlated to provider logs without changing result schemas per transport.
- OpenAI Agents Python now preserves `move_to` when coercing sandbox `apply_patch` operation mappings. Sandbox/file-edit harnesses should include mapping-shaped move operations, not only typed operation objects, so file-move tests catch lost destination paths before patch application mutates the workspace.
- OpenAI Agents Python docs now document passing OpenAI request fields through `ModelSettings.extra_args`, including `service_tier: "fast"` for supported Fast-mode models and `"priority"` equivalence. Latency/cost harnesses should record service-tier and extra-args provenance as part of run metadata, because a Fast-mode run is not comparable to default-tier output on latency or cost gates even when prompts and models match.
- OpenAI Agents JS v0.14.3 releases session-history transactions, tool-name collision handling, approved/guardrail result preservation, sibling tool cancellation, invalid-argument redaction, repeated-history provenance, and realtime/content-filter cleanup as a public JS baseline. Cross-SDK harness baselines can now pin these JS behaviors to a release instead of source-only commits.
- OpenAI Agents JS now redacts structured final-output validation failures by default and adds regression coverage for non-streamed, streamed, guardrail, and task/turn tracing paths. Privacy harnesses should treat final-output schema errors as a sensitive-data surface, asserting redacted operational messages while still preserving enough failure type/provenance for debugging.
- OpenAI Agents JS AI SDK extension fixes preserve response item order around tool calls, join final text across reasoning-separated message segments, and transform complete structured output once across interleaved reasoning/tool boundaries. Compatibility harnesses should include interleaved reasoning, multi-message final text, and tool-boundary structured-output fixtures so adapters do not drop or double-transform model-visible content.
- Google ADK now emits `additional_properties` schemas for typed `dict[K, V]` function parameters while leaving untyped dictionaries unconstrained. Tool-schema harnesses should include typed and untyped dict parameters so generated declarations preserve map value types without over-constraining unknown dictionaries.
- Google ADK now resolves Claude 5 model-name patterns to the Anthropic/Claude LLM class. Provider-routing harnesses should include newly released model-family patterns in routing fixtures, not just older Claude 3 and Claude 4 names, so new model aliases do not fall through to generic or incompatible backends.
- Google ADK's Live flow now scans all function responses in an event for `task_completed`, so a SequentialAgent live sub-agent can finish even when `task_completed` is called in parallel with other tools and its function response is not the first part. Live harnesses should include parallel tool-response ordering fixtures so completion/transfer control signals are detected by semantic response name rather than array position.

## Maintenance log
- 2026-08-06: reviewed OpenAI Agents Python atomic session/request-ID/apply-patch/Fast-mode commits, OpenAI Agents JS v0.14.3 and AI SDK/final-output redaction commits, and Google ADK typed-dict/model-routing/live-flow commits; appended notes on atomic replay-state persistence, transport-neutral request correlation, patch-move fixtures, service-tier provenance, released JS baselines, privacy-safe final-output errors, adapter ordering, typed map schemas, model-family routing, and order-insensitive live completion signals.

## 2026-08-08 review addendum
- OpenAI Agents Python now binds tool approvals to concrete tool invocations, including the originating call identity and invocation context, rather than treating approval as a loosely associated tool-level decision. Approval harnesses should attempt replay, cross-call, and concurrent-call approval mismatches and fail closed when the approval does not match the exact pending invocation.
- OpenAI Agents Python now counts model requests even when providers omit usage payloads, while preserving usage details when present. Cost and reliability harnesses should separate request-count evidence from token-usage evidence so missing provider usage does not erase a turn from rate, latency, or retry analysis.
- OpenAI Agents Python realtime tool-call item updates are now applied to session history. Realtime harnesses should compare live tool-call state with persisted replay history, including update-before-completion and resume-after-interruption cases.
- OpenAI Agents Python sandbox archive extraction now restores archived file modes. Workspace reproducibility harnesses should include executable, read-only, and permission-sensitive files and verify metadata after hydration, not only file contents.
- Google ADK now bounds jittered workflow retry delays by the configured `max_delay`. Retry harnesses should assert the final randomized delay never exceeds the declared ceiling, including at high retry counts and edge-valued jitter settings.
- Google ADK now preserves all tool results associated with parallel function calls in its OCI integration. Parallel-tool harnesses should assert result completeness and stable call/result association when multiple calls complete in one model turn.
- Google ADK's file artifact service now hardens metadata handling and partial writes. Artifact harnesses should verify tampered metadata is rejected and failed writes do not leave misleading partial artifacts or inconsistent metadata.
- OpenTelemetry's GenAI semantic-conventions reference work now permits conversation IDs on workflow spans and adds reference reports for client token usage and operation duration. Harnesses should carry conversation lineage through workflow spans while keeping client-operation metrics distinct from inference metrics.
- OpenTelemetry now warns that `gen_ai.tool.description` and `gen_ai.tool.definitions` may contain sensitive information. Telemetry harnesses should treat tool schemas as sensitive-data surfaces and verify redaction/opt-in behavior in traces, logs, and exported attributes.

## Maintenance log
- 2026-08-08: reviewed OpenAI Agents Python/JS reliability commits, Google ADK workflow/artifact/security commits, and OpenTelemetry GenAI workflow/reference/privacy commits; appended notes on invocation-bound approvals, usage-independent request accounting, realtime history persistence, archive metadata restoration, bounded retry jitter, parallel-result completeness, artifact integrity, conversation lineage, client-operation metrics, and sensitive tool-schema telemetry.

## 2026-08-09 review addendum
- OpenAI Agents Python now represents input admitted while resuming a run as durable `InputItem` occurrences with IDs, preserving pending input and current-step state when converting results back into `RunState`. Server-managed conversation input remains pending until a response confirms acceptance, and accepted versus unaccepted occurrences are reconciled explicitly. Harnesses should test interruption/resume, model-input filtering, server-acceptance failure, exactly-once admission, and pending-input round trips rather than treating resumed input as an ordinary transient prompt.
- OpenAI Agents Python now prunes orphaned tool outputs when enforcing limited session windows. Memory/replay harnesses should assert that truncation cannot retain a tool output without its originating call, and should compare limits by valid call/output units rather than raw item count.
- OpenAI Agents Python now lets applications approve unsafe model retries through an explicit replay-approval callback. Retry harnesses should classify replay safety separately from retry eligibility, record the approval decision and reason, and fail closed when an unsafe replay is neither approved nor rejected deterministically.
- OpenAI Agents Python preserves local shell outputs in `RunState` across resume. Sandbox harnesses should verify that completed local command output is replayable after interruption without rerunning the command or losing its tool-result provenance.
- OpenTelemetry's GenAI reference repository moved scenario execution under a pinned conformance runner. The runner starts the mock provider and live semantic-convention check, executes each scenario, validates emitted telemetry against `conformance.yaml`, and records committed per-scenario coverage data; violations warn by default but crashes or missing declared signals fail. Telemetry harnesses should prefer executable, pinned conformance fixtures over static schema snapshots.

## Maintenance log
- 2026-08-09: reviewed OpenAI Agents Python durable-resume/retry/session commits, OpenTelemetry's conformance-runner migration, and current OpenAI Agents JS activity; appended notes on durable input admission, replay approval, session-limit integrity, shell-output replay, and executable telemetry conformance.

## 2026-08-10 review addendum
- OpenAI Agents Python now serializes overlapping MCP manager lifecycle operations and gives connect/cleanup finite default timeouts, with tests covering concurrent cleanup/reconnect and bounded waits. Harnesses should assert single-execution cleanup, cleanup-error visibility, reconnect ordering, and timeout-bounded closeout rather than relying on unbounded teardown.
- OpenAI Agents Python's sandbox mount-redaction work preserves typed sandbox error classes and safe error-code/operation contracts while removing protected mount details from exception graphs. Privacy harnesses should verify both redaction and operational error-shape stability across async/sync boundaries and sandbox providers.
- OpenAI Agents Python now reports a transcription-session close failure to the consumer when no earlier terminal error exists, preventing a clean-looking voice stream from waiting forever for completion. Streaming harnesses should include post-success close failures and assert that the consumer receives a terminal error.
- OpenAI Agents JS now makes pending input durable in `RunState`, including identity-bearing admitted occurrences, explicit server-acceptance checkpoints, and context-filter mapping. Cross-SDK harnesses should test staged/admitted/accepted input states and exactly-once replay in both Python and JS.
- OpenAI Agents JS now preserves JSON-compatible structured tool outputs through `RunState` serialization/restoration instead of flattening them into strings. Replay/eval harnesses should compare structured output shape, reject non-JSON-compatible/cyclic values safely, and retain legacy string compatibility.
- Google ADK's CLI now auto-discovers `test_config.json` beside a single local eval-set file when no explicit config is supplied, while explicit config and eval-set IDs retain distinct behavior. Evaluation harnesses should test config precedence and record the resolved criteria file in run metadata.
- SWE-bench's first-party CLI now groups evaluation, reporting, image, and dataset operations; its report command can recompute verdicts from saved logs without starting containers. Benchmark harnesses should separate execution artifacts from grading so parser/criterion changes can be replayed without rerunning model instances.

## Maintenance log
- 2026-08-10: reviewed OpenAI Agents Python/JS lifecycle, sandbox, voice, RunState, and MCP commits; Google ADK eval CLI changes; and SWE-bench CLI changes. Appended notes on bounded serialized teardown, privacy-safe error contracts, terminal closeout propagation, cross-SDK durable replay, explicit eval-config resolution, and log-replayable benchmark grading.

## 2026-08-11 review addendum
- OpenAI Agents Python v0.20.0 and JS v0.15.0 are released baselines that consolidate durable pending input, structured tool-output replay, unsafe-replay approval, MCP v1/v2 compatibility, sandbox credential acknowledgement, and provider/result-fidelity fixes. Cross-SDK harnesses should pin release versions when comparing these behaviors rather than mixing release and source snapshots.
- OpenAI Agents JS added a historical `RunState` compatibility corpus with positive fixtures across prior schema versions and negative fixtures for future/malformed schemas, prototype-key input, and incompatible sandbox versions. Durable-state harnesses should maintain versioned migration fixtures and explicit rejection cases, not only round-trip the current serializer.
- Google ADK's `AgentEvaluator` can now persist one aggregated result artifact per eval set, saves results before failure assertions, and preserves positional-call compatibility by appending optional parameters. Eval harnesses should retain failed-run artifacts for diagnosis and record the resolved app/eval-set identity.
- Google ADK added property-based tests for metrics export, while OpenTelemetry clarified workflow-span criteria and added executable reference scenarios for LangGraph agents and OpenAI Agents. Telemetry harnesses should combine generated metric-invariant tests with pinned, library-specific reference scenarios rather than relying only on static schema checks.
- SWE-bench fixed three harness paths that could silently drop instances: default image tagging, browser-compatible seccomp settings, and replacement decoding for malformed test-output bytes. Benchmark harnesses should assert instance accounting from input through report and treat infrastructure/parser failures as visible outcomes, not missing examples.

## Maintenance log
- 2026-08-11: reviewed OpenAI Agents Python v0.20.0 and JS v0.15.0, Google ADK evaluation/telemetry/sandbox commits, OpenTelemetry GenAI workflow-reference updates, and SWE-bench harness fixes; appended notes on release-pinned durable-state baselines, migration corpora, failed-eval artifact persistence, executable telemetry conformance, and no-silent-drop benchmark accounting.

## 2026-08-12 review addendum
- OpenAI Agents JS added scripted model and sandbox-session utilities plus snapshot helpers for deterministic scenario tests. Harnesses can now exercise retries, approvals, streaming, handoffs, and sandbox behavior against controlled scripted responses before spending budget on live-model integration tests.
- OpenAI Agents Python now closes the provider model stream when a streamed turn ends in a terminal failure, and rejects non-positive voice audio-channel counts. Harnesses should assert resource teardown on terminal streamed failures and validate audio-shape inputs as setup failures rather than allowing malformed voice loops to start.
- Google ADK now validates path segments for GCS-backed evaluation-set and evaluation-result managers before constructing blob names. Shared evaluation storage should test traversal, separators, and null-byte rejection for every backend, not only local storage.
- LangGraph exposes `trace_policy` at `add_node` boundaries. Harnesses can make trace density a per-node policy, reducing low-value instrumentation while preserving explicit coverage for critical control-flow nodes.
- SWE-bench now materializes multimodal image assets after patch application and accepts the OpenLayers success marker. Multimodal benchmark harnesses should treat asset hydration and repository-specific success markers as explicit setup/grading phases and reconcile their outcomes.

## Maintenance log
- 2026-08-12: reviewed OpenAI Agents Python/JS, Google ADK, LangGraph, OpenTelemetry GenAI, and SWE-bench commits; appended notes on deterministic scripted testing, terminal stream teardown, evaluation-storage path isolation, node-level trace policy, and multimodal asset setup.

## 2026-08-13 review addendum
- OpenAI Agents Python's new public testing utilities were followed by a released-API contract fixture that freezes the testing module's public surface. Harness libraries should version and contract-test their test doubles, scripted models, and sandbox helpers rather than treating testing support as private implementation detail.
- OpenAI Agents Python now isolates interruption results when copying `RunState`, preventing one state copy's interruption list from mutating another. Durable harnesses should treat interruption collections as value state and test copy/serialize/restore isolation before resuming parallel or branched runs.
- OpenAI Agents Python adds a configurable ceiling to MCP retry backoff. Harnesses should record the effective retry ceiling and verify that jitter/backoff remains bounded, rather than judging retry behavior only from attempt counts.
- OpenAI Agents JS now accepts Standard Schema inputs and agent outputs across tools, handoffs, and final-output handling. Cross-language harnesses can use one schema abstraction, but should retain provider-specific validation/error fixtures because schema compatibility is now a broader adapter boundary.
- Google ADK expanded `load_skill` execute-tool telemetry with skill-related attributes and versioned golden scenarios, including invalid-schema, cache-hit, disabled-telemetry, and schema-v2 cases. Harnesses that load skills should assert both telemetry-disabled behavior and cache/validation attribution, not only tool success.
- Google ADK now fails an evaluation when an agent crashes before any metric runs. Evaluation harnesses should distinguish a crash/no-metric outcome from an empty or zero-scored metric result so infrastructure failures cannot appear as valid evaluations.
- Google ADK bounded the Apigee completions HTTP client timeout and redirects. Provider harnesses should make redirect policy and effective HTTP deadlines explicit, especially for streaming or long-running model calls.
- SWE-bench added an `assets_dir` path for local copies of URL-referenced binary assets. Multimodal benchmark harnesses should support deterministic local asset hydration and record whether an asset came from a pinned local cache or a remote URL.

## Maintenance log
- 2026-08-13: reviewed OpenAI Agents Python/JS, Google ADK, and SWE-bench commits; appended source-backed notes on public testing contracts, interruption-state isolation, bounded MCP backoff, Standard Schema adapters, skill telemetry goldens, no-metric eval failures, bounded provider HTTP behavior, and deterministic multimodal asset caches.

## 2026-08-14 review addendum
- OpenAI Agents Python now gives copied `RunState` checkpoints independent tool-decision ledgers and binds nested approval state to a new checkpoint scope. Harnesses should verify that approving one branched checkpoint cannot mutate or consume another branch's pending tool decision, including streamed and non-streamed restoration.
- OpenAI Agents Python now orders test spans by explicit start sequence rather than timestamps alone, and its public scripted sandbox session type is exposed for deterministic tests. Trace harnesses should use causal start order as the tie-breaker for same-clock spans and keep scripted sandbox fixtures on the supported public surface.
- OpenAI Agents JS now detaches `RunState` interruption snapshots and isolates interruption result arrays. Cross-SDK replay harnesses should treat pending interruptions as immutable snapshot values, not shared mutable arrays.
- Google ADK's parallel workflow worker now cancels in-flight item tasks when the worker is cancelled, then bounds cancellation draining with a five-second timeout. Loop harnesses should assert sibling cancellation and bounded orphan-task cleanup rather than only checking the parent failure.
- SWE-bench's current harness records the test command's own exit status and rejects logs that claim no failures when the command exited non-zero, closing a stdout-spoofing path. Benchmark harnesses should cross-check parsed results against an independently captured execution status.

## 2026-08-15 review addendum
- OpenAI Agents Python v0.21.0 and JS v0.16.0 now ship provider-neutral scripted testing utilities for agent, sandbox, realtime, and voice workflows, with public testing contracts and detached scripted snapshots. Hermes harnesses should split deterministic state-transition tests from live capability tests and contract-test the test-double surface itself.
- OpenAI Agents Python's released testing/runtime fixes isolate interruption snapshots and checkpoint tool-decision state, redact typed-output diagnostics, and bound MCP retry backoff. Harnesses should assert branch-safe snapshot isolation, privacy-safe failure artifacts, and effective retry ceilings rather than only final outputs.
- OpenAI Agents JS v0.16.0 preserves Chat Completions reasoning on its owning assistant message and keeps cached MCP tool arrays private from callers. Replay harnesses should preserve message ownership/order and prevent external mutation of discovery caches.

## Maintenance log
- 2026-08-15: reviewed OpenAI Agents Python v0.21.0 and JS v0.16.0 releases and same-day reliability commits; appended notes on deterministic provider-neutral testing, public testing contracts, checkpoint isolation, bounded retry policy, replay message ownership, and cache encapsulation.

## 2026-08-16 review addendum
- OpenAI Agents Python and JS now expose per-model-call timeouts, with the Python implementation distinguishing model-attempt timeout cancellation from caller cancellation, preserving timeout details in tracing, and avoiding unsafe stateful replay after a timed-out attempt. Harnesses should record effective per-attempt timeout, timeout-vs-caller-cancellation provenance, cleanup outcome, and whether retry was state-safe rather than treating all cancellations as equivalent.
- OpenAI Agents Python/JS add run-scoped sandbox working directories and Docker networking controls; the Python tests validate workspace-relative POSIX paths, revalidation of the working directory, and explicit messaging that a run cwd changes relative path resolution but does not isolate the rest of the session workspace. Sandbox harnesses should test cwd normalization, trust/revalidation, network policy, and cross-run visibility separately.
- SWE-bench now rejects zero-count runner summaries as evidence that a suite ran, preserves the recorded task repository during re-grading, and avoids rebuilding task images for report-only re-grades. Benchmark harnesses should require positive execution evidence, retain the exact graded test-repository identity, and separate execution from re-grading without mutating provenance.
- Google ADK's in-memory memory search now ranks matches by distinct query-word overlap and caps results at ten, preventing broad keyword matches from injecting most of the store into every prompt. Harnesses should test retrieval ranking and a hard prompt-size/result-count bound, not merely search recall.

## 2026-08-17 review addendum
- OpenAI Agents Python's sandbox `apply_patch` grammar now requires every `Update File` operation to contain at least one update hunk, including after an optional move, and rejects empty or move-only updates with a structured tool error. Patch-oriented harnesses should validate grammar-level preconditions before execution and distinguish rejected mutation requests from successful no-op edits.
- SWE-bench's Modal harness now refuses `--modal` combined with `--task-repo`, because Modal builds images remotely and would otherwise run tests from a task tree against an image built from a different source. The same change removes a misleading subprocess-local recursion-limit command, and CLI tests invoke the active interpreter via `sys.executable`. Benchmark harnesses should reject incompatible build/source combinations before work begins and verify that smoke tests exercise the interpreter/environment that will run the suite.
## 2026-08-18 review addendum

- OpenAI Agents Python's workflow-validation changes make approval scope, repository/submodule cleanliness, and regular-file identity executable harness contracts: runtime probes must disclose the exact command and capability scope before approval, review snapshots reject unresolved or dirty nested submodules, and evidence/manifests are read from one verified regular-file descriptor. Harnesses should preserve approval scope and source/build identity, reject symlink/FIFO/device substitutions, and validate recursive repository state before fingerprinting.
- Google ADK's evaluator now forks its metric registry per evaluation run before registering `EvalConfig` custom metrics, preventing one run's custom scoring functions from leaking into another while retaining globally registered evaluators. Eval harnesses should assert metric-registry isolation and preserve resolved metric configuration with each result.
- Google ADK's session service now deduplicates re-delivered events by equality, not only object/event identity, so copied broadcasts cannot double-apply state deltas. Session harnesses should replay equal event copies and verify idempotent state plus event-list behavior.
- Google ADK expanded `load_skill_resource` telemetry with resource-path attributes and schema-version goldens, while removing the prior skill cache-hit attribute. Telemetry harnesses should treat resource loads as attributable sub-operations and pin expected schema versions rather than assuming cache-hit fields remain stable.
- Google ADK hardened BigQuery tools with identifier validation, literal escaping, SELECT-only dry-run validation for dynamic subqueries, typed input checks, and differentiated validation/infrastructure errors. Tool harnesses should exercise injection, non-SELECT, malformed-parameter, and infrastructure-failure paths separately.
- SWE-bench 5.0.1 is a new released benchmark/harness baseline following the 5.0.0 changes tracked yesterday; Hermes should pin the benchmark version when comparing runs and record the release tag in evaluation provenance.

## 2026-08-20 review addendum
- OpenAI Agents Python now preserves request/token usage on the generation span before raising `ModelBehaviorError` for an empty completion truncated with `finish_reason="length"`; its MCP cache accessor also deep-copies tool models rather than only copying the list. Harnesses should retain usage even for terminal model-behavior failures and treat cached tool schemas as immutable snapshots.
- OpenAI Agents Python tracing now flushes registered processors even after tracing has been disabled. Shutdown tests should assert exporter drain independently of the enabled/disabled recording policy.
- OpenAI Agents JS now rejects unsuccessful Responses terminal states without losing billed usage, including usage from failed model attempts and retries. Cross-SDK harnesses should reconcile failure, retry, and usage records rather than dropping usage when no successful final response exists.
- OpenTelemetry GenAI semantic conventions now add modality-, cache-, and phase-aware usage breakdowns, rename cache creation to `gen_ai.usage.cache_write.input_tokens`, and explicitly document that detailed usage fields are subsets of aggregate totals. Hermes telemetry fixtures should test aggregate/subset accounting and avoid summing internal agent-tree breakdowns twice.
- SWE-bench 5.0.2 is the current released benchmark baseline; comparison harnesses should pin the release and record it in evaluation provenance.
- Temporal's new cost-governance guidance makes namespace/workload attribution, budget burn-rate tracking, and usage anomaly detection first-class operational controls using Billing API and OpenMetrics. Durable-agent harnesses should record cost attribution dimensions and closeout usage, not only latency and success.

## 2026-08-22 review addendum
- OpenAI Agents Python's voice API release work extends the released API-contract harness with public class/type-alias policies and canonical import checks, using a fixture to detect accidental changes to exported signatures and symbols. Hermes compatibility harnesses should pin public import paths and callable/type contracts for any runtime surface that downstream skills consume.
- OpenAI Agents JS now classifies an empty Chat Completions response ending with `finish_reason="length"` as a terminal model-behavior failure, marks it unsafe to replay, and retains response/usage evidence on the generation span; partial text, refusal, audio, reasoning, or function-call content remains valid output. Failure fixtures should distinguish replay safety and preserve billed usage rather than treating every truncated response as retryable or every failed response as unmeasured.
- Google ADK now records context-cache hit, fingerprint, content-count, and invocation-use attributes on LLM spans only when experimental telemetry is opted in. Harnesses should test both opt-in presence and default absence, and should keep cache identity/usage dimensions separate from ordinary inference usage.
- Google ADK's session migration now accepts bytes, bytearray, and memoryview representations for serialized event actions, preventing a successful PostgreSQL migration from silently losing state deltas. Migration harnesses should exercise driver-specific binary return types and verify action preservation after replay.

## 2026-08-23 review addendum
- OpenAI Agents Python expanded its released public API-contract fixture to cover public type aliases and canonical import paths, with generated policy data and tests detecting export/signature drift. Hermes compatibility harnesses should pin aliases and import locations as well as classes and callable signatures.
- OpenAI Agents Python and JS now strip internal `created_by` metadata when replaying RunItems as model input. Replay harnesses should distinguish internal provenance fields from provider-facing history and assert that framework-only ownership metadata is not accidentally sent back to the model.
- OpenAI Agents Python and JS deduplicate managed MCP server lifecycle ownership, preventing the same server from being closed or tracked through duplicate manager entries. MCP harnesses should assert single ownership and idempotent cleanup when discovery/configuration produces repeated server references.

## 2026-08-24 review addendum
- OpenAI Agents Python and JS now serialize exact current-response ownership for approval checkpoints, including the generated-item range and interruption identity, and reject older or malformed snapshots when that provenance cannot be proven. Harnesses should round-trip approval/output-guardrail state across repeated serialization, verify current-versus-historical call identity, and assert that unsafe snapshots fail before tool execution.

## 2026-08-25 review addendum
- OpenAI Agents Python's Unix-local sandbox now supports a configurable host-environment allowlist, while retaining manifest-provided variables and sandbox-owned overrides. Harnesses should assert the resolved environment rather than assuming host inheritance, and should verify that secrets and locale/runtime variables follow the configured isolation policy.
- OpenAI Agents Python now preserves a Responses `response_id` in redacted traces only for the official OpenAI endpoint, without retaining the full response payload. Trace harnesses should test both useful continuation lineage and endpoint trust gating, avoiding identifiers from untrusted compatible providers.
- Google ADK added workflow-level inference/tool-call counts and token-spend metrics, with experimental telemetry goldens covering opt-in versus opt-out behavior and nested workflow attribution. Hermes evals should distinguish per-agent metrics from workflow totals and pin telemetry configuration at run construction so environment changes cannot split one run's accounting.
- OpenAI Agents JS added server-wide MCP tool guardrails, making policy enforcement at the MCP-server boundary a released cross-SDK harness surface. Harnesses should test server defaults, per-tool overrides, and fail-closed behavior without conflating transport-wide policy with individual tool configuration.

## 2026-08-26 review addendum
- OpenAI Agents Python added a dedicated Docker-backed `containers` integration-test profile. It builds/tests the packaged wheel in an isolated environment with pinned Testcontainers plus Dapr/Redis dependencies, runs strict on GitHub-hosted ephemeral runners, explicitly removes containers, and emits per-profile JUnit results. Hermes release harnesses should separate container/service integration from ordinary tests, make unavailable prerequisites a failure in strict profiles, and retain environment/profile identity with pass/failure/skip counts.
- OpenAI Agents Python now redacts voice/STT transcription prompts from trace `model_config` when sensitive-data tracing is disabled while preserving the prompt in the provider request. Harnesses should assert privacy transformation and request behavior independently, rather than treating redaction as request mutation.
- OpenAI Agents Python's variadic tuple-schema fix preserves `tuple[T, ...]` as the item type for each variadic argument and adds validation/invocation coverage. Tool-contract harnesses should exercise generated schemas and parsed call arguments for variadic annotations, not only ordinary scalar parameters.

## 2026-08-27 review addendum
- OpenAI Agents Python now rejects `**kwargs` keys that collide with named keyword-bindable parameters as model behavior errors, instead of allowing a validated value to be replaced or producing an opaque Python multiple-values `TypeError`. Tool-contract harnesses should cover keyword-only, positional-or-keyword, context, and variadic combinations at the schema-to-call boundary.
- OpenAI Agents Python moved unbounded Unix-local workspace archive/extract and recursive-delete work off the async event loop while continuing to wait for the worker after cancellation, preventing a cancelled caller from releasing or clearing resources while a worker is still mutating the workspace. Harnesses should test both event-loop responsiveness and cancellation-before-worker-completion.
- OpenAI Agents Python's Chat Completions adapter now rejects Responses function-call outputs without a call ID before issuing a provider request, while the Responses adapter preserves those external-context outputs. Compatibility harnesses should assert adapter-specific preservation/rejection and the absence of an unsafe outbound request.
- OpenTelemetry's GenAI conformance runner now records structured `findings` and `entities` in reference scenario artifacts, including schema, missing-attribute, and unit findings. Harness closeout should retain machine-readable conformance findings rather than reducing a scenario to pass/fail alone.
- LangGraph thread-stream startup now accepts and forwards `langsmith_tracing` configuration. Stream harnesses should preserve tracing-project/example metadata at run creation so streamed and persisted traces remain attributable.

## 2026-08-29 review addendum
- Google ADK now terminates the entire process group for timed-out local commands, escalating from SIGTERM to SIGKILL after a bounded grace period and testing inherited-pipe cleanup. Sandbox harnesses should verify descendant-process termination, bounded timeout closeout, and absence of post-timeout workspace mutation—not just the parent process exit.
- Google ADK's workflow replay manager now extends its event index incrementally instead of rebuilding every bucket after each appended event, while checking the indexed prefix to detect compaction or rewind staleness. Replay benchmarks should cover both sub-quadratic append behavior and forced rebuilds after history mutation, with equivalence to a full rebuild as the correctness oracle.
- OpenAI Agents JS now records the terminal response ID and full response object on a traced streamed response before a consumer closes the stream at completion. Streaming harnesses should assert that early consumer close preserves response/tracing evidence and usage metadata, rather than treating successful output delivery as sufficient.

## 2026-09-01 review addendum
- Google ADK's database and SQLite session services now translate concurrent same-ID insert races into `AlreadyExistsError`, with tests covering one winner and one typed loser. Persistence harnesses should exercise the non-atomic check/insert race and classify duplicate creation as a stable domain outcome rather than leaking backend-specific integrity errors.
- Google ADK's Completions HTTP client now registers weak-reference exit cleanup, unregisters handlers on explicit close, and retains pending async cleanup tasks until completion. Long-lived harnesses should verify that cleanup ownership does not pin clients, that explicit close removes exit hooks, and that asynchronous teardown is observable rather than left to garbage collection.
- OpenTelemetry GenAI semantic conventions now deprecate per-message `gen_ai.output.messages[].finish_reason` in favor of positional `gen_ai.response.finish_reasons`, using `error` when an expected positional reason is missing. Telemetry harnesses should migrate assertions and preserve positional cardinality instead of inferring termination from output-message payloads.
- SWE-bench now closes Docker exec streams and their underlying responses on normal completion and timeout, with idempotent guarded cleanup tests. Benchmark harnesses should assert stream/socket closeout on both reader and timeout paths, including already-closed resources.
- SWE-bench's unreleased CLI changes separate inference logs under `logs/inference/<run_id>` from evaluation logs under `logs/evaluation/<run_id>`, and add submission packaging/verification commands. Benchmark harnesses should persist run identity and distinguish inference artifacts, evaluation artifacts, and submission validation rather than treating one output directory as the whole run.
- SWE-bench's new LiteLLM inference path filters by known context windows, retries calls with bounded exponential backoff, and fails loudly when pricing is unavailable instead of counting an unknown model as free. Cost-aware harnesses should record provider/model routing, filtering decisions, retry attempts, and pricing-resolution failures as explicit evaluation provenance.

## 2026-09-02 review addendum
- Google ADK's resumable workflow checkpoints no longer serialize `resume_inputs`; the implementation notes that credential-bearing resume inputs are reconstructed from persisted function responses, and a regression test verifies that a supplied API key is absent from checkpoint state. Durable harnesses should separate replay identity/status from secret-bearing continuation payloads, and should prove that resumed authorization still works without persisting the credential.
- Google ADK's LiteLLM/OpenAI-compatible schema adapters now preserve camelCase JSON-Schema aliases such as `anyOf` and `additionalProperties`, lower-case union type values, prefer an explicit `parameters_json_schema` over the legacy `parameters` field, and avoid copying `required` from the legacy schema when an explicit schema is present. Compatibility harnesses should test schema-shape fidelity at the provider boundary, especially unions and dual-schema declarations.

## 2026-09-03 review addendum
- Google ADK now validates the author of an auth-gated function call before resuming it from shared session history, skipping calls authored by another agent so that agent's own processor can handle its separately scoped tools. Multi-agent harnesses should include shared-session fixtures with colliding call IDs and assert author-bound resume, no cross-agent tool execution, and preservation of the user credential-recording path.
- Google ADK's tool-trajectory evaluator now supports `ignore_args`, independently of `EXACT`, `IN_ORDER`, or `ANY_ORDER` matching, so evaluators can score tool selection/order without requiring semantically irrelevant argument equality. Harnesses should record this criterion explicitly and keep argument-sensitive and argument-insensitive scores distinct.
